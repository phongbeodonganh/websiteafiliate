---
phase: 01-security-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/v1/seed/route.ts
  - src/lib/db/seed-mongodb.ts
  - src/lib/db/seed.ts
  - package.json
  - scripts/reset-admin.ts
  - DEPLOY.md
  - tests/api/security-regressions.test.ts
autonomous: true
requirements: [SEC-01]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No request to any seed path can wipe the database or reset admin credentials — the route, its lib helpers, and the npm entry point are all gone (D-01, D-02)"
    - "`npm run seed` no longer exists as a script; `scripts/seed-*.ts` + `scripts/create-admin.ts` are the only seeding/admin CLI paths (D-02)"
    - "A regression gate test asserts the former seed directory is absent and no API route performs an unfiltered destructive delete on user/settings models (D-03)"
    - "Admin password recovery is CLI-only: `npx tsx scripts/reset-admin.ts <username> <password>` upserts the admin with a bcrypt hash and is documented in DEPLOY.md (D-04)"
    - "The build and full test suite stay green after the deletions (no importer references the deleted modules)"
  artifacts:
    - "tests/api/security-regressions.test.ts — NEW: seed-absence + destructive-op scan + package.json seed-script-absence gates"
    - "scripts/reset-admin.ts — NEW CLI, modeled on scripts/create-admin.ts"
    - "DEPLOY.md — new 'Reset admin password (CLI)' section"
    - "DELETED: src/app/api/v1/seed/route.ts, src/lib/db/seed-mongodb.ts, src/lib/db/seed.ts"
  key_links:
    - "package.json scripts block → no `seed` entry pointing at deleted files (Pitfall 1)"
    - "tests/api/security-regressions.test.ts → walks src/app/api + src/app/api/v1/seed paths as the permanent regression gate"
    - "scripts/reset-admin.ts → hashPassword from src/lib/auth.ts + UserModel upsert (same contract as create-admin.ts)"
  assumptions:
    - "[SEC-01/concurrency|flagged-assumption] The three file deletions, the package.json script removal, and the gate test land in one commit — there is never an intermediate state where npm run seed points at a deleted file (probe: interrupted/parallel execution guarantee)"
    - "[SEC-01/e2e|flagged-assumption] Per research A6: no HTTP-level 404 test is needed for the deleted path — Next.js 404s unknown API routes by framework behavior; fs-absence + destructive-op grep gates are the accepted regression guards"
  prohibitions:
    - statement: "No web-reachable destructive endpoint may be (re-)introduced under any path — seed, wipe, reset, or any route that bulk-deletes users/settings"
      status: flagged-unverified
      verification: none
    - statement: "A replacement seed route must not be gated by query parameter, secret header, or IP allowlist — deletion is the locked decision (D-01), not hardening"
      status: flagged-unverified
      verification: none
    - statement: "No web path for admin password recovery may exist — recovery is CLI-only (D-04)"
      status: flagged-unverified
      verification: none
    - statement: "scripts/reset-admin.ts must not echo, log, or persist the plaintext password argument anywhere (console output shows only the username and user id)"
      status: flagged-unverified
      verification: none
    - statement: "Users and settings collections must never be deletable by an unauthenticated request, in any route, forever"
      status: flagged-unverified
      verification: none
---

<objective>
Delete the unauthenticated destructive seed surface completely — the `/api/v1/seed` route, its `seed-mongodb.ts` implementation, the dead `seed.ts` wrapper, and the `npm run seed` script — and stand up the phase's permanent regression gate test file. Move admin password recovery to a CLI script documented in DEPLOY.md (SEC-01; decisions D-01, D-02, D-03, D-04).

Purpose: A single public GET can currently wipe every collection and reset admin credentials. Total deletion (user decision: "dọn dẹp triệt để") plus a re-addition guard closes it permanently.

Output: three deleted files, cleaned package.json, `scripts/reset-admin.ts`, DEPLOY.md docs, `tests/api/security-regressions.test.ts` gate (extended by plan 04 with the secret gates).
</objective>

<execution_context>
@C:/Users/PC/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/PC/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-remediation/01-CONTEXT.md
@.planning/phases/01-security-remediation/01-RESEARCH.md
@.planning/phases/01-security-remediation/01-PATTERNS.md
</context>

## Artifacts this phase produces (plan 02)

- `tests/api/security-regressions.test.ts` — NEW gate test file (seed-absence, destructive-op scan, seed-script-absence); plan 04 extends it with secret gates
- `scripts/reset-admin.ts` — NEW CLI script (admin password recovery)
- `DEPLOY.md` — new reset-admin CLI documentation section
- Deletions: `src/app/api/v1/seed/route.ts`, `src/lib/db/seed-mongodb.ts`, `src/lib/db/seed.ts`
- `package.json` — `"seed"` script entry removed

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — delete the seed surface + prove absence with the regression gate</name>
  <files>src/app/api/v1/seed/route.ts, src/lib/db/seed-mongodb.ts, src/lib/db/seed.ts, package.json, tests/api/security-regressions.test.ts</files>
  <read_first>
    - src/app/api/v1/seed/route.ts — confirm it is the destructive route (deleteMany + admin upsert) being deleted wholesale
    - src/lib/db/seed-mongodb.ts and src/lib/db/seed.ts — scan exports before deletion to confirm nothing else imports them
    - package.json — line ~12 `"seed": "tsx src/lib/db/seed-mongodb.ts"` is removed in the SAME task (RESEARCH.md Pitfall 1)
    - tests/setup.ts — env contract for the vitest suite (JWT_SECRET set before imports)
    - .planning/phases/01-security-remediation/01-RESEARCH.md — §Code Examples "CI secret gate as a Vitest test" sketch (lines ~484–510) for the fs-walk gate shape
    - scripts/ directory listing — confirm `seed-*.ts` + `create-admin.ts` remain as the only seeding/admin paths
  </read_first>
  <action>
    1. `git rm src/app/api/v1/seed/route.ts src/lib/db/seed-mongodb.ts src/lib/db/seed.ts` (all three are git-tracked — verified). Then grep `src/` for any import or require of `seed-mongodb` or `db/seed` — research verified there are none; if any appear, remove the dead import lines too (do not keep the files).

    2. In `package.json`, delete the `"seed": "tsx src/lib/db/seed-mongodb.ts"` script entry in the same change (Pitfall 1: broken npm script otherwise). Do not add a replacement script — the content seeders under `scripts/` are run directly with tsx.

    3. Create `tests/api/security-regressions.test.ts` with three gates (D-03):
       - Seed-path absence: assert `src/app/api/v1/seed` directory does not exist (statSync throws).
       - Destructive-op scan: walk every `.ts` file under `src/app/api/` and assert no file performs an unfiltered destructive delete on the user or settings models — flag files where `deleteMany(` appears with an empty-or-wildcard filter argument on/near a `UserModel`/`SettingModel` reference. Tune the matcher to avoid false positives on legitimately-filtered deletes; if a real filtered `deleteMany` exists in some route, the gate must still pass (it targets unfiltered wipes only). Document the scan's carve-outs in a short comment inside the test.
       - npm-script absence: parse `package.json` and assert `scripts.seed` is undefined.

    4. Run the full suite to prove no importer broke (`npm test`), and a type check (`npx tsc --noEmit`) to catch any stale import the grep missed.
  </action>
  <verify>
    <automated>npx vitest run tests/api/security-regressions.test.ts</automated>
    <fails_when>non-zero exit code, "failed" in the vitest summary, or "0 passed" in the summary</fails_when>
  </verify>
  <done>The three seed files are gone, `npm run seed` errors with "missing script", the regression gate passes, and the full suite + type check are green.</done>
  <reversibility rating="reversible">git revert of this task's commit restores all three files and the script line with no data implications (nothing was migrated).</reversibility>
</task>

<task type="auto">
  <name>Task 2: CLI admin recovery — scripts/reset-admin.ts + DEPLOY.md (D-04)</name>
  <files>scripts/reset-admin.ts, DEPLOY.md</files>
  <read_first>
    - scripts/create-admin.ts — whole file (39 lines): the exact structural analog to copy (relative `../src/lib/...` imports, argv handling, usage-error exit 1 BEFORE any DB connection, upsert shape, top-level .catch, process.exit codes)
    - src/lib/auth.ts — confirm `hashPassword` is exported from here (create-admin imports it)
    - DEPLOY.md — the existing admin/CLI docs section; the new section goes next to it
  </read_first>
  <action>
    1. Create `scripts/reset-admin.ts` modeled 1:1 on `scripts/create-admin.ts`: takes `<username> <password>` from `process.argv`; if either is missing print a usage line (`Usage: tsx scripts/reset-admin.ts <username> <password>`) and exit 1 BEFORE `connectToDatabase()`; hash with `hashPassword`; `UserModel.findOneAndUpdate({ username }, { $set: { password_hash, role: 'admin', status: 'active' } }, { upsert: true, new: true })`; on success log ONLY the username and user id — never the password value; top-level `.catch` prints the error and exits 1. Relative imports (`../src/lib/db/mongodb`, `../src/lib/db/models`, `../src/lib/auth`) — scripts do not use `@/` aliases.

    2. In `DEPLOY.md`, add a `Reset admin password (CLI)` section next to the existing admin docs: command `npx tsx scripts/reset-admin.ts <username> <password>` run on the VPS in the app directory; note that it upserts (creates-or-resets) the admin and prints only the user id; note explicitly that no web path for password recovery exists by design (SEC-01/D-04).
  </action>
  <verify>
    <automated>Select-String -Path DEPLOY.md -Pattern "reset-admin" -Quiet</automated>
    <fails_when>returns False (no reset-admin documentation present in DEPLOY.md)</fails_when>
  </verify>
  <done>`npx tsx scripts/reset-admin.ts` with no args prints the usage line and exits 1 without touching the DB; with args it upserts the admin (smoke on VPS per VALIDATION.md manual list); DEPLOY.md documents the procedure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| open internet → /api/v1/* | Any unauthenticated request reaches every route; destructive capability must not exist here |
| VPS operator → CLI scripts | Recovery is an authenticated, on-box operation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-05 | Tampering/Elevation of Privilege | re-introduction of a destructive endpoint | critical | mitigate | fs-absence gate + destructive-op scan in tests/api/security-regressions.test.ts run on every `npm test`, which deploy.yml executes before build (Task 1) |
| T-1-06 | Elevation of Privilege | web-reachable password recovery | high | mitigate | CLI-only recovery (D-04); no route created; DEPLOY.md documents the CLI as the single path (Task 2) |
| T-1-SC | Tampering | npm installs | high | mitigate | none — zero packages installed in this plan |
</threat_model>

<verification>
- `npx vitest run tests/api/security-regressions.test.ts` green
- `npm test` full suite green; `npx tsc --noEmit` clean
- `npm run seed` → "missing script" error (expected)
- grep confirms zero references to the deleted modules in src/
</verification>

<success_criteria>
- An unauthenticated request to the former seed path is impossible (route deleted; 404 by framework) — ROADMAP success criterion 1
- No script/route path can bulk-delete users or settings without going through a CLI run by an operator
- The D-03 regression gate is permanent and runs in CI before every deploy
</success_criteria>

<output>
Create `.planning/phases/01-security-remediation/01-02-SUMMARY.md` when done
</output>
