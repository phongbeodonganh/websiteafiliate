---
phase: 01-security-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/blocked/page.tsx
  - src/app/api/v1/public/tracking/redirect/route.ts
  - src/lib/blacklist.ts
  - tests/api/tracking-redirect.test.ts
  - tests/api/blocked-page.test.ts
autonomous: true
requirements: [SEC-03, AFF-03]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A click on a blacklisted affiliate link returns HTTP 302 to /blocked?ref=<ClickLogId> with Cache-Control: no-store — the redirect behavior is unchanged, only the rendering mechanism (D-11)"
    - "/blocked resolves all blacklist data from the DB by the ref; no attacker-controlled text travels through the URL — only a 24-hex ObjectId (D-10)"
    - "Every blacklist-supplied string (project name, block reason, blocked countries) renders as a React text child; the rendered page never contains executable markup derived from data (SEC-03)"
    - "/blocked is a pure async server component: awaits searchParams, dynamic = 'force-dynamic', metadata robots { index: false, follow: false }, zero client JS, zero external script/image origins"
    - "Missing or invalid ref (non-24-hex ObjectId, or a repeated/array ref) redirects to / before any DB call — no empty state renders"
    - "ClickLog not found, link not found, or link no longer blacklisted → redirect('/') (race-tolerant re-resolution)"
    - "DB failure after a valid ref is accepted renders the fail-closed copy ('We couldn't verify this destination…') — never a crash screen, never a bounce to the offer"
    - "Blocked countries row is omitted entirely when the list is empty; 1..n countries render joined ', ' under the fixed 'Blocked countries' label"
    - "Default block reason when the DB reason is empty: 'Flagged by the AIDEALSUK safety review — fraud or unpaid commissions.' (English default per UI-SPEC planner note)"
    - "/blocked responses carry the site CSP from src/proxy.ts with zero proxy changes (matcher already covers non-/api paths)"
    - "Page copy is English (PROJECT.md constraint #8); blacklist data values render verbatim, never truncated or re-cased"
    - "Accessibility contract: <main> wraps content, H1 labelled via aria-labelledby, status mark aria-hidden, :focus-visible ring 3px #B91C1C offset 3px, hover-lift disabled under prefers-reduced-motion (inherited convention)"
  artifacts:
    - "src/app/blocked/page.tsx — async RSC BlockedPage with metadata + dynamic exports"
    - "tests/api/blocked-page.test.ts — state-matrix + XSS-contract + CSP assertions"
    - "src/app/api/v1/public/tracking/redirect/route.ts — blacklisted branch returns 302 to /blocked?ref=<clickLogId> (inline warningHtml deleted)"
    - "src/lib/blacklist.ts — English default block-reason fallback strings"
  key_links:
    - "redirect route blacklisted branch → 302 Location /blocked?ref=<clickLog._id> (ClickLog create result must be bound, not discarded)"
    - "/blocked → ClickLogModel.findById(ref) → AffiliateLinkModel.findById(log.affiliate_link_id) → checkUrlAgainstBlacklist(link.base_url)"
    - "src/proxy.ts matcher → CSP header on /blocked without any matcher edit"
  backstops:
    - statement: "Long receipt values and multi-country lists wrap via overflow-wrap: anywhere — never truncate and never scroll (UI-SPEC UI Considerations: overflow)"
      verification: backstop
    - statement: "Imported sheet strings (project_name / reason) render full-width with wrap — no ellipsis, no max-length clamp on security data (UI-SPEC UI Considerations: long-text)"
      verification: backstop
  assumptions:
    - "[SEC-03/ordering|flagged-assumption] Blocked countries render in stored array order (insertion order is the stable contract; no sorting applied)"
    - "[AFF-03/unclassified|flagged-assumption] Manual review of the unclassified probe: resolved to the D-10 contract itself — ref is the only URL-borne value and it is server-validated as a 24-hex ObjectId before use"
  prohibitions:
    - statement: "The blocked page must not render any blacklist-supplied string through a raw-HTML mechanism (dangerouslySetInnerHTML is banned on this page) — React text children only"
      status: flagged-unverified
      verification: none
    - statement: "No third-party origin may be introduced on /blocked — no external script tags, no cdn.tailwindcss.com Play CDN revival, no new image hosts (status mark is inline SVG)"
      status: flagged-unverified
      verification: none
    - statement: "CSP must not be weakened or worked around for /blocked — no matcher exception, no nonce-and-allowlist carving for third-party scripts"
      status: flagged-unverified
      verification: none
    - statement: "The redirect flow must never bounce a blacklisted click to the offer URL, under any error or race condition"
      status: flagged-unverified
      verification: none
    - statement: "No attacker-controlled text may travel through the /blocked URL — the only URL-borne value is the server-validated 24-hex ref"
      status: flagged-unverified
      verification: none
    - statement: "/blocked must not be indexable — robots noindex metadata must not be removed"
      status: flagged-unverified
      verification: none
    - statement: "DB errors on /blocked must not surface raw stack traces or internal error text to the visitor"
      status: flagged-unverified
      verification: none
---

<objective>
Replace the inline-HTML blacklist warning page (interpolated unescaped + Tailwind Play CDN, no CSP) with a real Next.js RSC route at `/blocked`, DB-backed via the ClickLog ref. This kills the stored-XSS vector structurally (React text children) and restores CSP coverage, preserving the 302-through-intermediate-page behavior (SEC-03, AFF-03; decisions D-09, D-10, D-11).

Purpose: A blacklisted-link warning page is the one public surface that renders attacker-influenced data (imported sheet strings). React escaping removes the injection class instead of patching it.

Output: `src/app/blocked/page.tsx`, rewired redirect route, updated + new tests, English fallback reason strings.
</objective>

<execution_context>
@C:/Users/PC/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/PC/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-remediation/01-CONTEXT.md
@.planning/phases/01-security-remediation/01-RESEARCH.md
@.planning/phases/01-security-remediation/01-PATTERNS.md
@.planning/phases/01-security-remediation/01-UI-SPEC.md

Framework contract (NOT training-data Next.js — read before writing page code):
@node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.mdx
</context>

## Artifacts this phase produces (plan 01)

- `src/app/blocked/page.tsx` — NEW: async RSC `BlockedPage`, `export const dynamic = 'force-dynamic'`, `export const metadata` with robots noindex
- `tests/api/blocked-page.test.ts` — NEW: state matrix, XSS-contract source assertions, proxy CSP assertion
- `src/app/api/v1/public/tracking/redirect/route.ts` — MOD: blacklisted branch deleted, replaced by `NextResponse.redirect(new URL('/blocked?ref=…', req.url), 302)`
- `src/lib/blacklist.ts` — MOD: two default-reason strings changed to the English default
- `tests/api/tracking-redirect.test.ts` — MOD: blacklisted-branch assertions moved to the 302→/blocked contract

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — blacklisted click → 302 /blocked?ref → inert RSC render</name>
  <files>src/app/blocked/page.tsx, src/app/api/v1/public/tracking/redirect/route.ts, src/lib/blacklist.ts, tests/api/tracking-redirect.test.ts, tests/api/blocked-page.test.ts</files>
  <read_first>
    - src/app/api/v1/public/tracking/redirect/route.ts — whole file: inline warningHtml block (lines ~54–95) is the delete target; healthy 302 branch (~98–104) is the response shape to copy; ClickLog create (~34–47) currently discards its result and must be bound to a variable
    - src/app/insider/success/page.tsx — the exact Next 16 analog: `searchParams` as awaited Promise + robots noindex metadata
    - src/app/insider/InsiderResult.tsx and src/app/insider/result.module.css — standalone editorial result-page pattern (centered shell, receipt rows, CTA) that the UI-SPEC locks as the layout precedent
    - src/app/globals.css — `:root` editorial token names (--editorial-canvas/surface/ink/copy/muted/divider) to use via Tailwind arbitrary values or classes
    - src/lib/brand.ts — BRAND_NAME constant (import, never hardcode the brand string)
    - src/lib/blacklist.ts — `checkUrlAgainstBlacklist` return shape (projectName, reason, blockedCountries) and the Vietnamese fallback strings at ~:100,120
    - src/lib/utils.ts lines 52–54 — existing `isValidObjectId` helper
    - tests/api/tracking-redirect.test.ts — current blacklisted-branch assertions (~lines 83–96) that assert the old inline HTML, plus the handler-invocation conventions used by the suite
    - .planning/phases/01-security-remediation/01-UI-SPEC.md — binding copywriting/spacing/typography/color/a11y contract for this page
    - .planning/phases/01-security-remediation/01-PATTERNS.md — sections "/blocked page (NEW)" and "redirect swap"
  </read_first>
  <action>
    Work test-first for the redirect contract, then build the page for keeps (production quality, not a prototype).

    1. RED: update `tests/api/tracking-redirect.test.ts` blacklisted branch to the new contract — assert status 302, a Location header whose path is `/blocked` with a `ref` query parameter matching 24 hex characters, and `Cache-Control: no-store`. Delete the assertions that expect the old inline HTML warning text. Run vitest; confirm the new assertions fail against current code.

    2. Rewire `src/app/api/v1/public/tracking/redirect/route.ts`: bind the ClickLog create result (lift it out of the Promise.all into a variable, keep the $inc inside the Promise.all); delete the entire warningHtml string and its Response return; in the blacklisted branch return `NextResponse.redirect(new URL(`/blocked?ref=${clickLog._id.toString()}`, req.url), 302)` with the Cache-Control no-store header set — absolute URL + explicit 302 are mandatory (NextResponse.redirect defaults to 307 and throws on relative URLs; see RESEARCH.md Pitfall 4).

    3. Create `src/app/blocked/page.tsx` as an async server component per the UI-SPEC contract: `export const dynamic = 'force-dynamic'`; metadata title `Link blocked for your safety` with `robots: { index: false, follow: false }`; `await searchParams` and accept only a string ref — absent or array-valued ref redirects to `/` via `redirect()` from `next/navigation` BEFORE any DB call; validate with `mongoose.isValidObjectId` (or the utils helper) → invalid redirects to `/`. Resolution chain: `connectToDatabase()` → `ClickLogModel.findById(ref)` → `AffiliateLinkModel.findById(log.affiliate_link_id)` → blocked = `link.status === 'blacklisted' || checkUrlAgainstBlacklist(link.base_url).isBlacklisted`; missing log/link or not blocked → `redirect('/')`. Render per UI-SPEC: masthead edition label `Security Notice`, eyebrow `Link blocked`, H1 `We blocked this link to protect you`, body sentence with the project name as a bolded React text child, receipt rows `Project` / `Block reason` / `Blocked countries`, default reason `Flagged by the AIDEALSUK safety review — fraud or unpaid commissions.`, primary CTA `Return to the safe homepage` (href `/`, min-height 48px), footer strip `AIDEALSUK / SECURITY` (left) and `LINK DISABLED` (right, accent red). Styling: standalone full-viewport editorial canvas (InsiderResult precedent) using globals.css tokens, lucide-react `ShieldAlert` as inline SVG status mark with the accent tint, hard offset shadow shell. Import BRAND_NAME from `@/lib/brand` for any brand mention. All data strings are React text children — no HTML interpolation of data anywhere.

    4. In `src/lib/blacklist.ts`, replace the two Vietnamese default-reason fallback strings (the `'Sàn lừa đảo / Không trả hoa hồng'` literals around lines 100 and 120) with `Flagged by the AIDEALSUK safety review — fraud or unpaid commissions.` (UI-SPEC planner note; PROJECT.md #8 public-content-is-English).

    5. Create `tests/api/blocked-page.test.ts` covering the populated path: seed a blacklisted link + click log in memory Mongo, invoke the page component with `searchParams: Promise.resolve({ ref })`, render the returned element to string (react-dom/server renderToStaticMarkup), assert the project name and reason appear as inert text, and assert that a reason value containing markup characters (seed the reason with a script-tag-shaped string) renders in entity-encoded form and the raw opening-bracket tag sequence never appears in the output. Also invoke `proxy()` from `src/proxy.ts` with a NextRequest to `http://localhost/blocked` and assert the response carries a content-security-policy header.

    Note: inside the page component use `redirect()` from `next/navigation` (per Next 16 bundled docs), not NextResponse — NextResponse.redirect is for route handlers only.
  </action>
  <verify>
    <automated>npx vitest run tests/api/tracking-redirect.test.ts tests/api/blocked-page.test.ts</automated>
    <fails_when>non-zero exit code, "failed" appearing in the vitest summary line, or "0 passed" in the summary</fails_when>
  </verify>
  <done>A blacklisted click 302s to /blocked?ref=<24-hex>; the page resolves the ref from the DB and renders every blacklist string as inert text under the site theme with CSP applied; no inline-HTML response exists anywhere in the redirect route.</done>
  <reversibility rating="reversible">Single-commit rewire of one route branch + one new page; git revert restores the prior inline-HTML behavior if the flow must be rolled back.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Expansion — boundary states + security contract assertions</name>
  <files>src/app/blocked/page.tsx, src/app/api/v1/public/tracking/redirect/route.ts, tests/api/blocked-page.test.ts</files>
  <read_first>
    - src/app/blocked/page.tsx — the just-built page (extend, don't rebuild)
    - tests/api/blocked-page.test.ts — extend the state matrix
    - .planning/phases/01-security-remediation/01-UI-SPEC.md — Copywriting Contract (fail-closed state copy), UI Considerations table (backstop rows: overflow + long-text), Interaction & Security Contract rules 1–9
    - src/app/api/v1/public/tracking/redirect/route.ts — the fallback branch (~lines 14–17) that returns 307 today
    - src/app/globals.css — prefers-reduced-motion block (inherit, do not redefine)
  </read_first>
  <action>
    1. State matrix in `tests/api/blocked-page.test.ts` + page behavior (each case invokes the page component and asserts):
       - ref supplied twice (array form in searchParams) → redirect to `/` thrown/observed before any DB call (spy or assert no model call happened).
       - unknown ref (valid 24-hex, no ClickLog row) → redirect to `/`.
       - link exists but is not blacklisted anymore (status active, blacklist check clean) → redirect to `/`.
       - reason empty in DB → page shows the default block reason string; countries empty → no `Blocked countries` receipt row exists in the rendered output at all.
       - DB failure: mock `connectToDatabase` (or the model call) to reject after a valid ref → rendered output contains the fail-closed body copy `We couldn't verify this destination, so it has been blocked as a precaution.`, `Project` row shows `Unavailable`, and the component does NOT throw NEXT_REDIRECT to the offer and does not emit an error screen.

    2. A11y pass on the page per UI-SPEC rule 8: `<main>` wraps the content, H1 is the labelled heading (aria-labelledby pointing at the H1 id), status mark wrapper has aria-hidden="true", CTA/link and interactive elements show a :focus-visible outline of 3px solid #B91C1C with 3px offset, and hover lift comes from the existing global convention (no new motion rules).

    3. Source-contract assertions appended to `tests/api/blocked-page.test.ts`: read `src/app/blocked/page.tsx` as text and assert (a) it contains no dangerouslySetInnerHTML occurrence, (b) it contains no absolute http(s) URL used as a script or image source (only relative hrefs and the inline SVG), (c) the metadata export includes the robots noindex keys.

    4. Normalize the redirect route's fallback branch (missing link, ~lines 14–17): change the implicit 307 responses to explicit `302` for consistency with the healthy and blacklisted branches (PATTERNS self-analog note), and confirm tests still pass.
  </action>
  <verify>
    <automated>npx vitest run tests/api/blocked-page.test.ts tests/api/tracking-redirect.test.ts</automated>
    <fails_when>non-zero exit code, or "failed" in the vitest summary line</fails_when>
  </verify>
  <done>All /blocked states behave per the UI-SPEC contract (invalid/unknown/race → home; DB failure → fail-closed copy; empty fields → omitted/defaulted), and the XSS + no-third-party-origin + noindex source contracts are enforced by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported sheet → blacklist DB → /blocked render | Attacker-influenced strings (project name, reason, countries) cross from a third-party spreadsheet into public HTML |
| visitor URL → /blocked ref param | Untrusted query input resolves into DB lookups |
| offer destination ← redirect route | Blacklisted destinations must never be bounced to |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Tampering | /blocked rendering of blacklist data | critical | mitigate | React text children only; raw-HTML interpolation banned; CSP active on the route; source-contract test enforces (Task 1/2) |
| T-1-02 | Spoofing | crafted ?ref= values | low | mitigate | 24-hex ObjectId validation before any DB call; unknown/invalid → redirect home (Task 1) |
| T-1-03 | DoS | DB failure on /blocked | low | mitigate | fail-closed generic copy; page never crashes or bounces to the offer (Task 2) |
| T-1-04 | Information disclosure | internal errors on /blocked | medium | mitigate | no stack traces/internal text in any rendered state (Task 2 assertions) |
| T-1-SC | Tampering | npm installs | high | mitigate | none — this plan installs zero packages (Research: zero-new-packages phase) |
</threat_model>

<verification>
- `npx vitest run tests/api/tracking-redirect.test.ts tests/api/blocked-page.test.ts` green
- `npm test` full suite green at wave merge (tracking-redirect changes can break nothing else)
- Manual spot check (optional): hit a blacklisted link locally and confirm the page renders themed, CSP header present in devtools, no network request to any cdn.* origin
</verification>

<success_criteria>
- Blacklisted click → 302 → /blocked?ref → warning page renders blacklist data as inert React text under the editorial theme with CSP (ROADMAP success criterion 4)
- Zero attacker-controlled text in URLs; zero third-party origins; zero raw-HTML interpolation on the page
- All UI-SPEC states implemented and asserted: populated, minimal (no countries), fail-closed, race/invalid → home
</success_criteria>

<output>
Create `.planning/phases/01-security-remediation/01-01-SUMMARY.md` when done
</output>
