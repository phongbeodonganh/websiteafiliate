---
phase: 01-security-remediation
plan: 04
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - src/lib/auth.ts
  - src/lib/gemini.ts
  - src/lib/db/models.ts
  - src/app/api/v1/cms/ai/generate-article/route.ts
  - src/app/api/v1/cms/blacklist/route.ts
  - src/app/api/v1/cms/blacklist/check/route.ts
  - src/app/api/v1/cms/blacklist/quick-blacklist/route.ts
  - src/app/api/v1/cms/blacklist/import/route.ts
  - src/app/api/v1/cms/blacklist/import-sheet-url/route.ts
  - src/app/api/v1/cms/settings/route.ts
  - tests/api/cms-auth-401.test.ts
  - tests/api/security-regressions.test.ts
autonomous: true
requirements: [SEC-02, AUTH-01]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: low

assumption_delta_decision:
  noun: "canonical JWT secret (single source in src/lib/auth.ts)"
  decision: "no-change"
  rationale: "The detector's pluralization signal ('fallback secrets') describes the bug this phase removes, not a new variant being added. The phase performs the promote itself: every route-local fallback secret is deleted and the single canonical JWT_SECRET in src/lib/auth.ts becomes the only verification key. No second identity source is introduced alongside it."

must_haves:
  truths:
    - "Every route under src/app/api/v1/cms/** verifies through the single canonical getAuthUser(req) from src/lib/auth.ts — zero route-local secrets, zero duplicate JWT logic, zero next/headers-based guards (AUTH-01)"
    - "A JWT signed with any previously-hardcoded fallback secret is rejected with 401 by every CMS route (parameterized proof)"
    - "A logout-blacklisted token is rejected with 401 uniformly across all CMS routes — the canonical guard consults the token blacklist (uniform revocation, success criterion 2)"
    - "src/ contains zero secret literals: no Gemini key fallback, no duplicated fallback key in the AI route, no leaked Google Cloud project ID in any error string (SEC-02, D-05)"
    - "Missing required secrets fail lazily at first use with an actionable message naming the env var — never at import/startup; build, CI, and tests never need real secrets (D-06)"
    - "Gemini key precedence (D-07): request-supplied userApiKey → env GEMINI_API_KEY → settings gemini_api_key (typed schema field, no cast)"
    - "GET /api/v1/cms/settings returns a masked key hint (last 4 chars) or omits the key — never the raw gemini_api_key"
    - "GET and PUT /api/v1/cms/settings both require getAuthUser (planner decision per research A4 — public settings remain at /api/v1/public/settings)"
    - "The parameterized 401 test covers every authed CMS route without a token, including the previously-unauthenticated blacklist/check and the now-locked settings routes"
    - "JWT sessions stay in localStorage — no httpOnly cookie migration in this phase (D-15 locked boundary)"
  artifacts:
    - "tests/api/cms-auth-401.test.ts — NEW: parameterized no-token 401 table over the full CMS route inventory + fallback-secret-token rejection + logout-blacklist uniformity"
    - "tests/api/security-regressions.test.ts — EXTENDED: jwt-verify/sign location gate + secret-fallback-literal gate + leaked-project-ID-absence gate (D-08)"
    - "src/lib/db/models.ts — SettingSchema + ISetting gain gemini_api_key (D-07 enabler)"
    - "src/app/api/v1/cms/settings/route.ts — auth-guarded GET/PUT, masked key in GET, gemini_api_key accepted in PUT"
    - "src/lib/gemini.ts + cms/ai/generate-article/route.ts — fallback literals removed, D-07 precedence chain active"
  key_links:
    - "6 CMS routes → import { getAuthUser } from '@/lib/auth' (src/lib/auth.ts itself stays canonical/untouched in behavior)"
    - "generate-article key chain → userApiKey || process.env.GEMINI_API_KEY || settings.gemini_api_key (typed read)"
    - "SettingSchema.gemini_api_key ← PUT /api/v1/cms/settings → masked hint out via GET"
    - "tests/api/security-regressions.test.ts → src/ walk enforcing jwt-verify/sign locality + secret fallback absence, run in CI before deploy"
  assumptions:
    - "[SEC-02/concurrency|flagged-assumption] Literal removals, the D-07 chain, the schema field, and the gate test land within this one plan — no intermediate commit leaves a secret read throwing without its replacement source (probe: interrupted/parallel guarantee; the gate test going red is the detector)"
    - "[AUTH-01/concurrency|flagged-assumption] Logout revocation is per-request: a token blacklisted mid-session is rejected on its next request; in-flight handlers complete normally (accepted semantics of the in-memory blacklist)"
    - "[AUTH-01/settings-consumers|flagged-assumption] Per research A4: no unauthenticated consumer depends on GET /api/v1/cms/settings — the public use case is served by /api/v1/public/settings; if a hidden consumer surfaces at runtime, the 401 will reveal it and the public route is the fix path"
    - "[SEC-02/settings-ui|flagged-assumption] Per research A5: the admin settings UI input for gemini_api_key may not exist yet — the API accepts and stores the field (env stays primary), UI wiring is not part of this phase"
  prohibitions:
    - statement: "No secret literal may be (re-)introduced as a fallback default on any required secret read anywhere in src/"
      status: flagged-unverified
      verification: none
    - statement: "JWT verification and signing calls must not appear outside src/lib/auth.ts (enforced by the D-08 gate test, scoped to src/)"
      status: flagged-unverified
      verification: none
    - statement: "Missing secrets must not fail at import or startup time — lazy call-time throw only; build/CI/tests must never require real secrets (D-06 forbids startup validation)"
      status: flagged-unverified
      verification: none
    - statement: "The raw Gemini key must never be returned by GET /api/v1/cms/settings — masked hint (last 4 chars) or omission only (that GET previously had no auth)"
      status: flagged-unverified
      verification: none
    - statement: "The route-local duplicate guard shape (next/headers headers() + route-local secret) must not be preserved anywhere — canonical getAuthUser(req) with the handler's request object only"
      status: flagged-unverified
      verification: none
    - statement: "The logout token-blacklist check must not be bypassed by any CMS auth path — uniform revocation across all routes (success criterion 2, Pitfall 6)"
      status: flagged-unverified
      verification: none
    - statement: "JWT session storage must not migrate to httpOnly cookies in this phase — D-15 keeps localStorage; logout revocation + rate limiting are the in-scope mechanisms"
      status: flagged-unverified
      verification: none
    - statement: "Role checks (admin/editor/author) must not be weakened or dropped during the auth swap — preserve each route's existing role matrix verbatim"
      status: flagged-unverified
      verification: none
    - statement: "The public settings route (/api/v1/public/settings) must not be locked — only the CMS settings route is guarded"
      status: flagged-unverified
      verification: none
---

<objective>
Make CMS auth unforgeable and source secret-free: swap the 5 route-local fallback-secret JWT guards (plus the unauthenticated `blacklist/check`) to the canonical `getAuthUser(req)`, remove all secret literals with a D-07-compliant Gemini key precedence chain, mask the settings key read, and enforce both permanently with the parameterized 401 suite and the D-08 regression gates (SEC-02, AUTH-01; decisions D-05, D-06, D-07, D-08, D-15).

Purpose: SEC-02 regressed once in this exact codebase via a route-local fallback secret; canonicalization plus gates that run in CI before every deploy prevent recurrence instead of hoping.

Output: 7 CMS routes canonicalized, secrets purged, `gemini_api_key` schema field + masked settings API, two new/extended gate test files.
</objective>

<execution_context>
@C:/Users/PC/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/PC/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-remediation/01-CONTEXT.md
@.planning/phases/01-security-remediation/01-RESEARCH.md
@.planning/phases/01-security-remediation/01-PATTERNS.md
</context>

## Artifacts this phase produces (plan 04)

- `tests/api/cms-auth-401.test.ts` — NEW: route-inventory table (globbed at execution time), no-token 401 parameterization, fallback-secret token rejection, logout-blacklist uniformity
- `tests/api/security-regressions.test.ts` — EXTENDED (file created in plan 02): jwt locality gate, secret-fallback gate, leaked-project-ID gate
- `src/lib/db/models.ts` — `SettingSchema.gemini_api_key` field + `ISetting` interface field
- `src/app/api/v1/cms/settings/route.ts` — getAuthUser on GET+PUT, `gemini_api_key_masked` in GET response, key accepted in PUT
- `src/lib/gemini.ts` — fallback key literal + leaked project ID removed; bilingual fail-fast message
- `src/app/api/v1/cms/ai/generate-article/route.ts` — canonical guard + D-07 precedence chain
- `src/app/api/v1/cms/blacklist/{route,check,quick-blacklist,import,import-sheet-url}/route.ts` — canonical guard (check gains auth for the first time)

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — canonical auth swap on 7 CMS routes + fallback-secret 401 proof</name>
  <files>src/app/api/v1/cms/blacklist/route.ts, src/app/api/v1/cms/blacklist/check/route.ts, src/app/api/v1/cms/blacklist/quick-blacklist/route.ts, src/app/api/v1/cms/blacklist/import/route.ts, src/app/api/v1/cms/blacklist/import-sheet-url/route.ts, src/app/api/v1/cms/ai/generate-article/route.ts, src/app/api/v1/cms/settings/route.ts, tests/api/cms-auth-401.test.ts</files>
  <read_first>
    - src/lib/auth.ts — whole file: canonical `getAuthUser(req)` (~:48–55), `verifyToken` with the logout-blacklist check (~:38–44). DO NOT modify this file's behavior.
    - .planning/phases/01-security-remediation/01-PATTERNS.md — section "Canonical JWT guard": the exact per-route delete-block (import of headers from next/headers, the route-local secret constant, the verifyAdminAuth function) and the call-site migration table (blacklist/route.ts call sites ~:28, :57, :109; generate-article ~:28 reads headers() instead of req — the swap must pass the handler's request object)
    - Each of the 7 route files — read fully before editing; preserve every role check verbatim (generate-article: admin|editor; blacklist writes: admin; settings PUT: whatever it does today — verify)
    - tests/setup.ts — test JWT_SECRET set at module top before imports
    - tests/api/tracking-redirect.test.ts — direct handler-invocation conventions
    - .planning/phases/01-security-remediation/01-RESEARCH.md — Pitfall 6 (logout-blacklist asymmetry), Open Question 1 (blacklist/check), Open Question 2 + A4 (settings locking)
  </read_first>
  <action>
    1. In each of `cms/blacklist/route.ts`, `cms/blacklist/quick-blacklist/route.ts`, `cms/blacklist/import/route.ts`, `cms/blacklist/import-sheet-url/route.ts`, `cms/ai/generate-article/route.ts`: delete the route-local secret constant, the local verify function, and the now-unused imports (headers from next/headers, the jsonwebtoken import). Replace every `const user = await verifyAdminAuth()` call site with `const user = getAuthUser(req)` — synchronous, receiving the handler's own request object (for generate-article that is its NextRequest). Keep each route's existing 401/403 envelopes and role checks exactly as they are.

    2. In `cms/blacklist/check/route.ts` (read-only lookup, currently zero auth — research Open Question 1): add `getAuthUser(req)` + the standard 401 envelope at the top of the POST handler. Do not relocate the route.

    3. In `cms/settings/route.ts` (planner decision per research A4): guard BOTH GET and PUT with `getAuthUser` + the standard 401 envelope. In GET, replace any raw key exposure: return `gemini_api_key_masked` (last 4 characters prefixed with a bullet run) when a key is set, and never the raw field. In PUT, accept an optional `gemini_api_key` string and persist it (admin-only path — verify the PUT's existing role check and add the admin check if absent).

    4. Create `tests/api/cms-auth-401.test.ts`:
       - Build the route inventory by globbing `src/app/api/v1/cms/**/route.ts` at execution time; document the resulting method+path table in the test file. Assert EVERY authed verb on every route returns 401 when invoked with a Request lacking an Authorization header (direct handler invocation per suite conventions; dynamic routes get a dummy id). The settings routes are included (locked in step 3) — no exclusions.
       - Fallback-secret rejection: craft tokens signed with the two former route-local fallback literals (copy them from the 01-PATTERNS.md excerpts — the blacklist-routes literal and the generate-article literal; do not invent new values) and assert 401 on all six swapped routes.
       - Logout uniformity: sign a canonical token (via the auth.ts signToken export or the jsonwebtoken call in the test using the test secret), add it to the token blacklist through the exported tokenBlacklist API, and assert 401 on one canonical route AND on generate-article (Pitfall 6: the swap must extend revocation, not just the secret).
       - Non-Bearer Authorization header (e.g. Basic scheme) → 401, treated identically to a missing header.

    5. Sanity-run the auth-login tests — the canonical login flow must be untouched.
  </action>
  <verify>
    <automated>npx vitest run tests/api/cms-auth-401.test.ts tests/api/auth-login.test.ts</automated>
    <fails_when>non-zero exit code, "failed" in the vitest summary, or "0 passed" in the summary</fails_when>
  </verify>
  <done>All CMS routes (including blacklist/check and settings) 401 without a token and reject fallback-secret tokens; logout-revoked tokens fail uniformly; login flow unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Secrets purge + Gemini key precedence chain (D-05/D-06/D-07) + D-08 gates</name>
  <files>src/lib/gemini.ts, src/app/api/v1/cms/ai/generate-article/route.ts, src/lib/db/models.ts, src/app/api/v1/cms/settings/route.ts, tests/api/security-regressions.test.ts</files>
  <read_first>
    - src/lib/gemini.ts lines ~60–130 — the options destructure carrying the default-parameter key literal (~:75) and the existing fail-fast throw (~:79–81); lines ~190–208 — the error string containing the leaked Google Cloud project ID (~:202)
    - src/app/api/v1/cms/ai/generate-article/route.ts lines ~85–100 — the duplicated key literal (~:91) and the phantom settings read (`(dbSetting as any).geminiApiKey`, always undefined — RESEARCH.md Pitfall 2)
    - src/lib/db/models.ts lines ~250–298 (ISetting + SettingSchema) and ~313–324 (hot-reload-safe model export) — the exact shapes to extend
    - src/lib/auth.ts lines ~9–14 — the lazy-getter pattern (getJwtSecret) that D-06 mandates extending, not replacing
    - tests/api/security-regressions.test.ts — the gate file created in plan 02; extend, don't restructure
    - .planning/phases/01-security-remediation/01-RESEARCH.md — Pitfall 2 (phantom settings field), Pitfall 5 (gate must not ban benign defaults), §Code Examples gate sketch
  </read_first>
  <action>
    1. `src/lib/gemini.ts`: remove the default-value literal from the destructured `apiKey` option so the existing fail-fast activates for every caller; sharpen the throw message to name both key sources (env GEMINI_API_KEY, or the CMS settings Gemini API Key field — bilingual, following the existing message style). In the ~:199–205 error string, remove the leaked Google Cloud project number and replace with generic guidance (enable the Generative Language API; no project identifier).

    2. `cms/ai/generate-article/route.ts`: rebuild the key resolution per D-07 as `request userApiKey → process.env.GEMINI_API_KEY → settings.gemini_api_key` with a TYPED settings read (no `as any` cast — the new schema field makes it type-safe). The fallback literal disappears entirely; when all three sources are empty, return the route's standard error envelope pointing at the two sources (env or settings).

    3. `src/lib/db/models.ts`: add `gemini_api_key: { type: String }` to SettingSchema and the optional matching field to the ISetting interface, following the file's existing field/comment conventions.

    4. `tests/api/security-regressions.test.ts` — add three gates (D-08; extend the plan-02 file):
       - JWT locality: walk `src/**/*.ts(x)` and assert JWT verify/sign calls occur ONLY in `src/lib/auth.ts` (implement the walker per the RESEARCH.md sketch; exclude nothing else).
       - Secret-fallback literals: for the secret env names (JWT_SECRET, GEMINI_API_KEY, MONGODB_URI, INSIDER_TOKEN_SECRET, RESEND_API), assert no quoted fallback literal follows the env read — implement the window-scoped check per the RESEARCH sketch and Pitfall 5 so benign defaults (page numbers, labels) cannot trip it. Scope: `src/` only (tests legitimately set dummy secrets).
       - Leaked-project-ID absence: assert the removed project number appears nowhere in `src/`.
       - Do NOT touch `src/lib/db/mongodb.ts` (the env-file parsing fallback is load-bearing on the VPS — research Open Question 3: leave untouched this phase).

    5. Run the full suite and a production build (`npm run build`) — both must succeed with zero real secrets present (D-06 lazy failure; tests/setup.ts provides the test JWT secret).
  </action>
  <verify>
    <automated>npx vitest run tests/api/security-regressions.test.ts tests/api/cms-auth-401.test.ts; if ($?) { npm run build }</automated>
    <fails_when>non-zero exit from either command, "failed" in a vitest summary, or any build error output</fails_when>
  </verify>
  <done>Zero secret literals remain in src/ (gates prove it), the D-07 chain resolves env-then-settings with typed reads, settings GET masks the key, and build + suite pass without real secrets.</done>
  <reversibility rating="reversible">Revert restores the fallback literals; runtime impact of the change is intended (tokens signed with old fallback secrets start failing 401 — success criterion 2), and the VPS canonical secret is unaffected.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Swap-integrity sweep — full suite, type check, secret-surface grep</name>
  <files>tests/api/cms-auth-401.test.ts</files>
  <read_first>
    - tests/api/cms-auth-401.test.ts — the route table built in Task 1 (fix any inventory drift discovered here)
    - .planning/phases/01-security-remediation/01-RESEARCH.md — Validation Architecture sampling rates (per-task file run; per-wave full suite)
  </read_first>
  <action>
    1. Run the full suite (`npm test`) and `npx tsc --noEmit`; fix any fallout from the swap — likely candidates are suites that invoked the swapped handlers with header-object quirks or that relied on the duplicates accepting fallback-signed tokens.

    2. Confirm the cms-auth-401 route table matches the actual glob count (no route silently skipped); if the glob discovers a route added since research, add it to the assertions rather than excluding it.

    3. Grep sweep as a human-readable double-check: report (do not silently pass) any remaining occurrence in `src/` of route-local JWT usage or quoted fallbacks on secret env reads — the D-08 gates already fail on these, so this sweep is belt-and-braces; paste the zero-hit result into the summary.
  </action>
  <verify>
    <automated>npm test; if ($?) { npx tsc --noEmit }</automated>
    <fails_when>non-zero exit from either command, or "failed" in the vitest summary</fails_when>
  </verify>
  <done>Full suite + type check green; the parameterized table covers the complete CMS route inventory; zero secret-fallback hits across src/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attacker-crafted JWT → CMS routes | Tokens signed with any fallback secret previously crossed as valid admin sessions |
| logged-out token → CMS routes | Revoked tokens previously kept working on 5 routes |
| admin settings PUT → settings singleton | The Gemini key transits this boundary; GET must not echo it |
| source tree → git history | Secret literals must not re-enter the tracked source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-09 | Spoofing | forgeable admin JWT via fallback secrets | critical | mitigate | canonical single-secret verification (Task 1) + fallback-token 401 proof + D-08 jwt-locality gate (Task 2) |
| T-1-10 | Spoofing | logged-out token reuse on duplicate-guard routes | high | mitigate | canonical getAuthUser consults the logout blacklist; uniformity asserted in the 401 suite (Task 1) |
| T-1-11 | Information disclosure | GET /api/v1/cms/settings exposing settings metadata / raw Gemini key | high | mitigate | lock GET+PUT behind getAuthUser; masked key hint only (Task 1) |
| T-1-12 | Elevation of Privilege | unauthenticated cms/blacklist/check | medium | mitigate | getAuthUser added; included in the parameterized 401 table (Task 1) |
| T-1-13 | Information Disclosure | secret re-entry via future code | high | mitigate | secret-fallback + jwt-locality + project-ID gates run in CI before every deploy (Task 2) |
| T-1-SC | Tampering | npm installs | high | mitigate | none — zero packages installed in this plan |
</threat_model>

<verification>
- `npx vitest run tests/api/cms-auth-401.test.ts` green (full inventory, fallback-secret + logout cases)
- `npx vitest run tests/api/security-regressions.test.ts` green (plan-02 gates + three new D-08 gates)
- `npm test` and `npx tsc --noEmit` green; `npm run build` succeeds with no secrets in env
- Success criteria 2 + 3 of ROADMAP Phase 1 demonstrated by the suite
</verification>

<success_criteria>
- A JWT self-signed with any previously-hardcoded fallback secret is rejected 401 by every CMS route; logout revocation applies uniformly (ROADMAP success criterion 2)
- Source contains zero secret literals; missing secrets fail fast with actionable errors; D-08 gates prevent recurrence (ROADMAP success criterion 3)
</success_criteria>

<output>
Create `.planning/phases/01-security-remediation/01-04-SUMMARY.md` when done
</output>
