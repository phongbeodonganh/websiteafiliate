---
phase: 01-security-remediation
plan: 07
type: execute
wave: 3
depends_on: ["01-02", "01-05"]
files_modified:
  - src/lib/utils.ts
  - src/lib/homepage-articles.ts
  - src/lib/blacklist.ts
  - src/app/api/v1/public/articles/route.ts
  - tests/lib/client-ip.test.ts
  - tests/lib/redos.test.ts
  - DEPLOY.md
autonomous: true
requirements: [SEC-04]
user_setup:
  - service: nginx-vps
    why: "X-Forwarded-For must be overwritten (not appended) on the VPS so getClientIp's last-hop parse sees exactly one attacker-unforgeable value (D-14) — the Nginx config lives outside git"
    dashboard_config:
      - task: "Apply the DEPLOY.md §8 directive in /etc/nginx/sites-available/websiteafiliate inside BOTH the 80 and 443 server blocks, then run nginx -t and systemctl reload nginx"
        location: "VPS shell — file documented in DEPLOY.md §8; dual-block warning in §9"

estimate:
  tokens: 34000
  raw_tokens: 17000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "getClientIp returns the LAST X-Forwarded-For entry (spoofed first entries ignored), falls back to X-Real-IP, defaults to 127.0.0.1 with no headers (D-14)"
    - "A crafted ReDoS search pattern against /api/v1/public/articles?search= returns quickly (time-boxed assertion) and matches literally only — escapeRegExp applied at both user-text→RegExp sites (public search + blacklist sweeper)"
    - "Search terms longer than 100 characters skip the regex filter gracefully (defense in depth; no 400, no hang)"
    - "The rate-limit suite from plan 05 stays green after the parse flip — its single-entry XFF isolation headers resolve identically under first- and last-hop parsing"
    - "DEPLOY.md §8 documents the XFF overwrite directive ($remote_addr, not append) with the manual VPS application steps for both 80/443 blocks"
  artifacts:
    - "tests/lib/client-ip.test.ts — NEW: last-hop/first-entry-spoofed/X-Real-IP/default matrix"
    - "tests/lib/redos.test.ts — NEW: evil-pattern timing + literal-match assertions"
    - "src/lib/utils.ts — getClientIp last-hop fix; escapeRegExp relocated here as an export"
    - "DEPLOY.md — §8 XFF overwrite directive + VPS manual steps"
  key_links:
    - "articles route + blacklist sweeper → escapeRegExp imported from @/lib/utils (single source)"
    - "getClientIp last-hop result → plan 05's limiter keys (subscribe:/click:/redirect:) — same export, harder-to-spoof value"
    - "DEPLOY.md §8 → VPS nginx directive change (manual step, tracked in user_setup)"
  assumptions:
    - "[SEC-04/nginx|flagged-assumption] Per research A3: the VPS Nginx config matches the DEPLOY.md §8 template (single Nginx, no CDN in front); if a CDN/extra proxy sits in front, last-hop trust assumptions change — deploy checklist verifies with nginx -T"
    - "[SEC-04/encoding|flagged-assumption] The 100-char search cap counts UTF-16 code units via .length — good enough for a DoS cap; no grapheme-cluster semantics needed"
  prohibitions:
    - statement: "The first X-Forwarded-For entry must not be trusted as the client IP — last trusted hop only, paired with the Nginx overwrite (D-14)"
      status: flagged-unverified
      verification: none
    - statement: "No user-text→RegExp site may remain unescaped — both the public articles search and the blacklist sweeper get escapeRegExp (CONCERNS #6)"
      status: flagged-unverified
      verification: none
    - statement: "getClientIp's exported signature and the '127.0.0.1' no-header default must not change — plan 05's limiter and existing tests key on them"
      status: flagged-unverified
      verification: none
    - statement: "PM2 ecosystem.config.js (instances:1, fork mode) must not be changed — in-memory limiter prerequisite"
      status: flagged-unverified
      verification: none
---

<objective>
Neutralize the remaining public-input abuse vectors: fix IP trust by parsing the LAST X-Forwarded-For hop (paired with the Nginx overwrite directive in DEPLOY.md §8) and kill the ReDoS vector by relocating `escapeRegExp` to a shared home and applying it at every user-text→RegExp site with a 100-char cap (SEC-04; decision D-14).

Purpose: A spoofed XFF header defeats plan 05's limiter keying, and a crafted regex can hang the server — both are unauthenticated attack surfaces.

Output: last-hop `getClientIp`, shared `escapeRegExp`, hardened articles search + blacklist sweeper, two new lib test files, DEPLOY.md §8 directive.
</objective>

<execution_context>
@C:/Users/PC/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/PC/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-remediation/01-CONTEXT.md
@.planning/phases/01-security-remediation/01-RESEARCH.md
@.planning/phases/01-security-remediation/01-PATTERNS.md
@.planning/phases/01-security-remediation/01-05-SUMMARY.md
</context>

## Artifacts this phase produces (plan 07)

- `src/lib/utils.ts` — `getClientIp` last-hop fix; `escapeRegExp` relocated from homepage-articles.ts and exported
- `src/lib/homepage-articles.ts` + `src/app/api/v1/public/articles/route.ts` + `src/lib/blacklist.ts` — escapeRegExp + 100-char cap
- `tests/lib/client-ip.test.ts`, `tests/lib/redos.test.ts` — NEW
- `DEPLOY.md` — §8 XFF overwrite directive + VPS manual steps

<tasks>

<task type="auto">
  <name>Task 1: IP trust fix — last-hop getClientIp + client-ip matrix + Nginx overwrite docs</name>
  <files>src/lib/utils.ts, tests/lib/client-ip.test.ts, DEPLOY.md</files>
  <read_first>
    - src/lib/utils.ts lines ~52–67 — isValidObjectId + the current getClientIp (trusts the FIRST XFF entry — the spoofable target); keep the signature and the '127.0.0.1' default
    - tests/lib/sanitize.test.ts — unit-test conventions (flat describes, behavior-named its)
    - DEPLOY.md §8 and §9 — the Nginx template ($proxy_add_x_forwarded_for today) and the dual-block warning
    - .planning/phases/01-security-remediation/01-05-SUMMARY.md — the limiter keys built on getClientIp (single-entry XFF test isolation)
    - .planning/phases/01-security-remediation/01-PATTERNS.md — section "utils.ts (getClientIp last-hop + escapeRegExp relocation)"
  </read_first>
  <action>
    1. `getClientIp` in `src/lib/utils.ts`: return the LAST XFF entry (`forwarded.split(',').pop()!.trim()`), keep the X-Real-IP fallback and the '127.0.0.1' default and the exported signature unchanged (D-14 code half).

    2. Create `tests/lib/client-ip.test.ts`: single XFF entry → that entry; multi-entry XFF → LAST entry returned even when the first entry is a spoofed value; no XFF but X-Real-IP present → X-Real-IP; neither → 127.0.0.1.

    3. `DEPLOY.md` §8: replace the `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;` directive with `proxy_set_header X-Forwarded-For $remote_addr;` plus a short comment that the header is overwritten, not appended; add the manual VPS steps (edit `/etc/nginx/sites-available/websiteafiliate` in BOTH the 80 and 443 server blocks per §9, `nginx -t`, `systemctl reload nginx`) and the post-deploy verification curl from 01-VALIDATION.md (spoofed XFF header must not reach ClickLog.ip_address).
  </action>
  <verify>
    <automated>npx vitest run tests/lib/client-ip.test.ts tests/api/rate-limit.test.ts; if ($?) { Select-String -Path DEPLOY.md -Pattern "X-Forwarded-For \$remote_addr" -Quiet }</automated>
    <fails_when>non-zero exit from vitest, "failed" in a vitest summary, or Select-String returning False (directive not documented)</fails_when>
  </verify>
  <done>Last-hop IP parsing live with the full matrix pinned, plan 05's rate-limit suite still green (order-invariant isolation confirmed), DEPLOY.md carries the overwrite directive + VPS steps.</done>
</task>

<task type="auto">
  <name>Task 2: escapeRegExp relocation + ReDoS hardening at both RegExp sites</name>
  <files>src/lib/utils.ts, src/lib/homepage-articles.ts, src/lib/blacklist.ts, src/app/api/v1/public/articles/route.ts, tests/lib/redos.test.ts</files>
  <read_first>
    - src/lib/homepage-articles.ts lines ~28–30 (the local escapeRegExp to relocate) and ~42–45 (the already-fixed usage shape to copy)
    - src/app/api/v1/public/articles/route.ts lines ~30–40 — the unescaped `new RegExp(searchKeyword.trim(), 'i')` site (public, unauthenticated)
    - src/lib/blacklist.ts lines ~135–144 — the sweeper's unescaped RegExp site (admin/sheet input)
    - .planning/phases/01-security-remediation/01-PATTERNS.md — section "ReDoS fix sites"
    - .planning/phases/01-security-remediation/01-RESEARCH.md — SEC-04 rows in the Test Map (evil-pattern + time-box shape)
  </read_first>
  <action>
    1. Relocate `escapeRegExp` to `src/lib/utils.ts` as an exported function (regex body identical to the homepage-articles.ts original); `homepage-articles.ts` imports it from `@/lib/utils` and deletes its local copy.

    2. Apply at both remaining sites: `articles/route.ts` wraps the trimmed search keyword in `escapeRegExp(...)` before `new RegExp(..., 'i')`, and skips the regex filter entirely when the trimmed keyword exceeds 100 characters (graceful defense-in-depth — no 400, search just doesn't filter); `blacklist.ts` sweeper wraps its `rootDomain || hostname || targetDomainOrUrl` argument the same way.

    3. Create `tests/lib/redos.test.ts`: an evil-pattern input (nested-quantifier shape) against the escapeRegExp-wrapped builder returns literally and a direct timed invocation of the search path completes well under a generous time-box (assert elapsed < 2000ms and correct literal matches); over-100-char keyword yields an unfiltered result set rather than an error.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/redos.test.ts; if ($?) { npx vitest run tests/api/insider-lifecycle.test.ts }</automated>
    <fails_when>non-zero exit from either command, or "failed" in a vitest summary</fails_when>
  </verify>
  <done>escapeRegExp is the single shared source, both user-text→RegExp sites are escaped with the 100-char cap, and the timed ReDoS assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-supplied XFF header → getClientIp | Attacker-controlled header previously decided the limiter key |
| public search query → RegExp engine | Unauthenticated input previously compiled as a raw regex |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-16 | Spoofing | XFF first-hop forgery defeating limiter keying | high | mitigate | last-hop parse in getClientIp (Task 1) + Nginx overwrite directive + manual VPS step (Task 1, user_setup) |
| T-1-17 | DoS | ReDoS via public search | high | mitigate | escapeRegExp at both sites + 100-char cap + timed test (Task 2) |
| T-1-SC | Tampering | npm installs | high | mitigate | none — zero packages installed in this plan |
</threat_model>

<verification>
- `npx vitest run tests/lib/client-ip.test.ts tests/lib/redos.test.ts` green
- `npx vitest run tests/api/rate-limit.test.ts tests/api/insider-lifecycle.test.ts tests/api/tracking-redirect.test.ts` still green (no regressions from the IP parse flip)
- `npm test` full suite green at wave merge
- Manual (VPS, post-deploy): apply the §8 directive; spoofed-XFF curl no longer pollutes ClickLog.ip_address (01-VALIDATION.md manual item)
</verification>

<success_criteria>
- A crafted ReDoS search pattern does not hang the server — ROADMAP success criterion 5, second clause
- Spoofed XFF first entries cannot influence limiter keying or ClickLog.ip_address (with the VPS Nginx step applied)
</success_criteria>

<output>
Create `.planning/phases/01-security-remediation/01-07-SUMMARY.md` when done
</output>
