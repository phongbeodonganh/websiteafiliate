---
phase: 01-security-remediation
plan: 05
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/lib/rateLimit.ts
  - src/lib/utils.ts
  - src/lib/homepage-articles.ts
  - src/lib/blacklist.ts
  - src/app/api/v1/public/articles/route.ts
  - src/app/api/v1/public/insider/route.ts
  - src/app/api/v1/public/tracking/click/route.ts
  - src/app/api/v1/public/tracking/redirect/route.ts
  - tests/api/rate-limit.test.ts
  - tests/lib/client-ip.test.ts
  - tests/lib/redos.test.ts
  - tests/api/insider-lifecycle.test.ts
  - tests/api/tracking-redirect.test.ts
  - DEPLOY.md
autonomous: true
requirements: [SEC-04]
user_setup:
  - service: nginx-vps
    why: "X-Forwarded-For must be overwritten (not appended) on the VPS so getClientIp's last-hop parse sees exactly one attacker-unforgeable value (D-14) — the Nginx config lives outside git"
    dashboard_config:
      - task: "Apply the DEPLOY.md §8 directive in /etc/nginx/sites-available/websiteafiliate inside BOTH the 80 and 443 server blocks, then run nginx -t and systemctl reload nginx"
        location: "VPS shell — file documented in DEPLOY.md §8; dual-block warning in §9"

estimate:
  tokens: 82000
  raw_tokens: 41000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Subscribe (POST /api/v1/public/insider and its /subscribe alias) over-limit returns a hard 429 with a Retry-After header; the first 5 requests per IP per 60s window succeed (D-12, D-13)"
    - "Click/redirect over-limit NEVER returns 429 — over-cap requests still return their success envelope/302 but skip the analytics writes (D-13)"
    - "A duplicate click (same IP + affiliate link within the 60s dedupe window) creates no additional analytics: no extra ClickLog insert (click route) and no extra click_count $inc — F5/back-forward cannot inflate counts"
    - "Concurrent duplicate clicks within the dedupe window produce exactly one counted click (Node's single-threaded event loop serializes Map updates; asserted by test)"
    - "A blacklisted redirect still 302s to /blocked?ref even when dedupe/flood-cap is active — the ClickLog ref anchor is always created on the redirect route (design decision below)"
    - "getClientIp returns the LAST X-Forwarded-For entry (spoofed first entries ignored), falls back to X-Real-IP, defaults to 127.0.0.1 with no headers (D-14)"
    - "A crafted ReDoS search pattern against /api/v1/public/articles?search= returns quickly (time-boxed assertion) and matches literally only — escapeRegExp applied at both user-text→RegExp sites (public search + blacklist sweeper)"
    - "Search terms longer than 100 characters skip the regex filter gracefully (defense in depth; no 400, no hang)"
    - "The existing failure-lockout limiter used by the login route is untouched — the new count-based limiter is additive in the same module"
    - "DEPLOY.md §8 documents the XFF overwrite directive ($remote_addr, not append) with the manual VPS application steps for both 80/443 blocks"
  artifacts:
    - "src/lib/rateLimit.ts — NEW exports: ConsumeResult, consumeRequest(key, limit, windowMs), consumeDedupe(key, windowMs), _resetForTests()"
    - "tests/api/rate-limit.test.ts — NEW: subscribe 429 boundary + window expiry + click/redirect dedupe + flood-cap skip behavior"
    - "tests/lib/client-ip.test.ts — NEW: last-hop/first-entry-spoofed/X-Real-IP/default matrix"
    - "tests/lib/redos.test.ts — NEW: evil-pattern timing + literal-match assertions"
    - "src/lib/utils.ts — getClientIp last-hop fix; escapeRegExp relocated here as an export"
    - "DEPLOY.md — §8 XFF overwrite directive + VPS manual steps"
  key_links:
    - "insider POST → consumeRequest(`subscribe:${getClientIp(req)}`, 5, 60000) → 429 + Retry-After (login-route envelope shape)"
    - "click/redirect handlers → consumeRequest(`click|redirect:<ip>`, 60, 60000) + consumeDedupe(`<ip>:<linkId>`, 60000) → skip writes, keep response"
    - "redirect route blacklisted branch → /blocked?ref=<clickLog._id> (from plan 01) — dedupe must not remove the ref anchor"
    - "articles route + blacklist sweeper → escapeRegExp imported from @/lib/utils (single source)"
    - "DEPLOY.md §8 → VPS nginx directive change (manual step, tracked in user_setup)"
  assumptions:
    - "[SEC-04/boundary|flagged-assumption] Thresholds (subscribe 5/60s; click/redirect flood cap 60/60s; 60s dedupe) are the research-recommended starting points, delegated by the user — tunable constants at the top of the integration sites; boundary behavior (Nth allowed, N+1th → policy) is what the tests pin"
    - "[SEC-04/empty|flagged-assumption] Requests with no IP headers key on the preserved '127.0.0.1' default — existing tests rely on it (Pitfall 3)"
    - "[SEC-04/encoding|flagged-assumption] The 100-char search cap counts UTF-16 code units via .length — good enough for a DoS cap; no grapheme-cluster semantics needed"
    - "[SEC-04/precision|flagged-assumption] Window math uses integer milliseconds via Date.now(); Retry-After is ceil'd to whole seconds — no fractional-second precision contract"
    - "[SEC-04/dedupe-vs-ref|flagged-assumption] Design decision: on the redirect route the ClickLog row is ALWAYS created (it is the /blocked ref anchor from plan 01) and only the $inc is skipped on dedupe/over-cap; on the click route (no ref consumer) both insert and $inc are skipped. If interrupted mid-click the worst case is one extra uncounted log row"
    - "[SEC-04/state-loss|flagged-assumption] In-memory limiter/dedupe state resets on deploy (pm2 delete + start in deploy.yml) and on restart — accepted, self-heals; PM2 instances:1 fork mode is the hard prerequisite and must not change"
  prohibitions:
    - statement: "Click/redirect over-limit must not return 429 to real users — records are dropped and responses still succeed (D-13)"
      status: flagged-unverified
      verification: none
    - statement: "Duplicate clicks (same IP + link within the dedupe window) must not inflate click analytics — no insert and no increment within the window"
      status: flagged-unverified
      verification: none
    - statement: "The first X-Forwarded-For entry must not be trusted as the client IP — last trusted hop only, paired with the Nginx overwrite (D-14)"
      status: flagged-unverified
      verification: none
    - statement: "PM2 ecosystem.config.js (instances:1, fork mode) must not be changed — it is the hard prerequisite for the in-memory limiter and token blacklist"
      status: flagged-unverified
      verification: none
    - statement: "The existing failure-lockout limiter API used by the login route must not be removed or broken — extend rateLimit.ts, never replace it"
      status: flagged-unverified
      verification: none
    - statement: "No user-text→RegExp site may remain unescaped — both the public articles search and the blacklist sweeper get escapeRegExp (CONCERNS #6)"
      status: flagged-unverified
      verification: none
    - statement: "The /subscribe alias path must not bypass the subscribe rate limit — one handler, both paths covered"
      status: flagged-unverified
      verification: none
    - statement: "No Redis or external store may be introduced for the limiter/dedupe this phase — in-memory Map per the deferred-items decision"
      status: flagged-unverified
      verification: none
---

<objective>
Close the public-endpoint abuse holes: rate-limit all three public write endpoints with the right policy per endpoint (hard 429 for subscribe; silent record-dropping for click/redirect), fix IP spoofing via last-hop parsing + Nginx overwrite docs, and neutralize the ReDoS vector by relocating and applying `escapeRegExp` at every user-text→RegExp site (SEC-04; decisions D-12, D-13, D-14).

Purpose: Unauthenticated bursts can currently flood the insider list and inflate click analytics, a crafted regex can hang the server, and a spoofed XFF header defeats any limiter keying.

Output: extended rateLimit.ts, three hardened public routes, escapeRegExp applied everywhere, IP fix, three new test files, DEPLOY.md §8 directive.
</objective>

<execution_context>
@C:/Users/PC/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/PC/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-remediation/01-CONTEXT.md
@.planning/phases/01-security-remediation/01-RESEARCH.md
@.planning/phases/01-security-remediation/01-PATTERNS.md
@.planning/phases/01-security-remediation/01-01-SUMMARY.md
@.planning/phases/01-security-remediation/01-02-SUMMARY.md
</context>

## Artifacts this phase produces (plan 05)

- `src/lib/rateLimit.ts` — new exports `ConsumeResult`, `consumeRequest`, `consumeDedupe`, `_resetForTests` (failure-lockout API untouched)
- `src/lib/utils.ts` — `getClientIp` last-hop fix; `escapeRegExp` relocated from homepage-articles.ts and exported
- `src/app/api/v1/public/insider/route.ts` — subscribe hard 429 (covers the /subscribe alias via re-export)
- `src/app/api/v1/public/tracking/{click,redirect}/route.ts` — flood cap + dedupe with silent-skip policy
- `src/app/api/v1/public/articles/route.ts` + `src/lib/blacklist.ts` — escapeRegExp + 100-char cap
- `tests/api/rate-limit.test.ts`, `tests/lib/client-ip.test.ts`, `tests/lib/redos.test.ts` — NEW
- `DEPLOY.md` — §8 XFF overwrite directive + VPS manual steps

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — count-based limiter + subscribe hard 429 end-to-end</name>
  <files>src/lib/rateLimit.ts, src/app/api/v1/public/insider/route.ts, tests/api/rate-limit.test.ts, tests/api/insider-lifecycle.test.ts</files>
  <read_first>
    - src/lib/rateLimit.ts — whole file: the existing failure-lockout API (checkRateLimit/recordFailedAttempt/resetRateLimit), module-level Map conventions, single-instance comment (~:11–12), interface style (~:13–18). These exports stay byte-identical in behavior — login depends on them
    - src/lib/tokenBlacklist.ts lines ~17–24 — the lazy-prune-on-read pattern to mirror
    - src/app/api/v1/auth/login/route.ts lines ~6–21 — the only existing limiter integration: pre-check shape + tooManyRequests 429 envelope with Retry-After header
    - src/app/api/v1/public/insider/route.ts — the POST handler (first lines of its try block get the consume call)
    - src/app/api/v1/public/subscribe/route.ts — the alias re-export; one file, both paths covered
    - tests/api/insider-lifecycle.test.ts — the suite that hits subscribe repeatedly from the default IP (Pitfall 3)
    - .planning/phases/01-security-remediation/01-RESEARCH.md — Pattern 3 (count-based limiter shape + suggested thresholds), Pitfall 3 (test reset affordance)
    - .planning/phases/01-security-remediation/01-PATTERNS.md — sections "insider route (+ /subscribe alias)" and "rateLimit.ts (add count-based limiter)"
  </read_first>
  <action>
    1. Extend `src/lib/rateLimit.ts` (keep every existing export untouched): add `export interface ConsumeResult { allowed: boolean; retryAfterSeconds?: number }`; add `export function consumeRequest(key: string, limit: number, windowMs: number): ConsumeResult` — module-level `Map<string, { count: number; windowStart: number }>`, reset the window when `now - windowStart >= windowMs`, increment otherwise, return `allowed: false` with a ceil'd `retryAfterSeconds` once count exceeds limit; prune stale entries on touch (tokenBlacklist pattern). Add `export function consumeDedupe(key: string, windowMs: number): boolean` — returns true when the key was already seen within the window (records lastSeenAt, prunes stale entries). Add `export function _resetForTests(): void` clearing the new maps only. Copy the module's single-instance comment style citing ecosystem.config.js instances:1/fork as prerequisite.

    2. In `insider/route.ts` POST, as the first statement of the try block: `consumeRequest(`subscribe:${getClientIp(req)}`, 5, 60_000)`; on `!allowed` return the 429 envelope modeled on the login route's helper — `{ status: 'error', message: 'Quá nhiều yêu cầu. Vui lòng thử lại sau ít phút.' }` with the Retry-After header set from retryAfterSeconds. The /subscribe alias inherits via its re-export — no change to that file.

    3. Create `tests/api/rate-limit.test.ts`: boundary case (5 subscribe requests from one IP succeed, 6th returns 429 with an integer Retry-After ≥ 1); window expiry lets the next request through (use a short windowMs via direct consumeRequest calls with fake timers or a tiny window); `_resetForTests()` clears state between scenarios (call it in beforeEach); the /subscribe alias path is limited identically (invoke the subscribe module's POST handler six times). Invoke handlers directly per suite conventions with distinct X-Forwarded-For headers where scenarios need isolation.

    4. Update `tests/api/insider-lifecycle.test.ts` to call `_resetForTests()` in beforeEach so the existing lifecycle suite cannot trip the new 5/60s subscribe limit (Pitfall 3).
  </action>
  <verify>
    <automated>npx vitest run tests/api/rate-limit.test.ts tests/api/insider-lifecycle.test.ts</automated>
    <fails_when>non-zero exit code, "failed" in the vitest summary, or "0 passed" in the summary</fails_when>
  </verify>
  <done>Subscribe 429s at the cap with Retry-After on both paths; existing lifecycle suite stays green with the reset affordance in place.</done>
  <reversibility rating="reversible">Thresholds are constants at the integration sites; reverting the handler lines restores pre-phase behavior instantly.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Click/redirect flood cap + 60s dedupe — silent skip, no 429</name>
  <files>src/app/api/v1/public/tracking/click/route.ts, src/app/api/v1/public/tracking/redirect/route.ts, tests/api/rate-limit.test.ts, tests/api/tracking-redirect.test.ts</files>
  <read_first>
    - src/app/api/v1/public/tracking/click/route.ts — the write pair (~:40–51 ClickLog create + $inc click_count) and the success envelope (~:53–56) that must still return on skip
    - src/app/api/v1/public/tracking/redirect/route.ts — post-plan-01 state: ClickLog create is BOUND (the /blocked ref anchor), blacklisted branch 302s to /blocked?ref, healthy branch 302s to the offer
    - src/lib/rateLimit.ts — the Task-1 consumeRequest/consumeDedupe exports (reuse, don't duplicate)
    - tests/api/rate-limit.test.ts — extend with click/redirect cases
    - tests/api/tracking-redirect.test.ts — the blacklisted-branch contract from plan 01
    - .planning/phases/01-security-remediation/01-RESEARCH.md — Pattern 3 thresholds; D-13 policy in 01-CONTEXT.md
  </read_first>
  <action>
    1. Both tracking handlers: compute `const ip = getClientIp(req)` once. Flood cap: `consumeRequest(`click:${ip}` / `redirect:${ip}`, 60, 60_000)`. Dedupe: `consumeDedupe(`${ip}:${affiliateLinkId}`, 60_000)`. When either signals skip:
       - click route: skip BOTH the ClickLog insert and the click_count $inc; still return the normal success envelope (no 429, no behavior change visible to the caller).
       - redirect route: ALWAYS create the ClickLog (it is the /blocked ref anchor — plan 01's contract), skip only the click_count $inc; the blacklist check and all branches (302 to /blocked, 302 to offer, fallback) proceed exactly as before.
       Order: the flood-cap/dedupe check runs BEFORE the write pair in both handlers.

    2. Extend `tests/api/rate-limit.test.ts`: two rapid clicks same IP+link → exactly one counted analytics write (assert via ClickLogModel.countDocuments for the click-route path and via the link's click_count for redirect); 61st click within the window → still 200 with no new write; concurrent duplicate clicks (Promise.all of N same-IP+link clicks) → click_count incremented exactly once.

    3. Extend `tests/api/tracking-redirect.test.ts`: a blacklisted click WITH dedupe active still 302s to /blocked?ref=<24-hex> (the ref anchor guarantee) and click_count was not incremented on the duplicate.
  </action>
  <verify>
    <automated>npx vitest run tests/api/rate-limit.test.ts tests/api/tracking-redirect.test.ts</automated>
    <fails_when>non-zero exit code, or "failed" in the vitest summary line</fails_when>
  </verify>
  <done>F5/back-forward and bursts cannot inflate analytics; real users never see a 429 on click/redirect; blacklisted interception is unaffected by dedupe.</done>
</task>

<task type="auto">
  <name>Task 3: IP trust fix + escapeRegExp everywhere + Nginx overwrite docs</name>
  <files>src/lib/utils.ts, src/lib/homepage-articles.ts, src/lib/blacklist.ts, src/app/api/v1/public/articles/route.ts, tests/lib/client-ip.test.ts, tests/lib/redos.test.ts, DEPLOY.md</files>
  <read_first>
    - src/lib/utils.ts lines ~52–67 — isValidObjectId + the current getClientIp (trusts the FIRST XFF entry — the spoofable target); keep the signature and the '127.0.0.1' default
    - src/lib/homepage-articles.ts lines ~28–30 (the local escapeRegExp to relocate) and ~42–45 (the already-fixed usage shape to copy)
    - src/app/api/v1/public/articles/route.ts lines ~30–40 — the unescaped `new RegExp(searchKeyword.trim(), 'i')` site (public, unauthenticated)
    - src/lib/blacklist.ts lines ~135–144 — the sweeper's unescaped RegExp site (admin/sheet input)
    - DEPLOY.md §8 and §9 — the Nginx template ($proxy_add_x_forwarded_for today) and the dual-block warning
    - tests/lib/sanitize.test.ts — unit-test conventions (flat describes, behavior-named its)
    - .planning/phases/01-security-remediation/01-PATTERNS.md — section "utils.ts (getClientIp last-hop + escapeRegExp relocation)" and "ReDoS fix sites"
  </read_first>
  <action>
    1. `getClientIp` in `src/lib/utils.ts`: return the LAST XFF entry (`forwarded.split(',').pop()!.trim()`), keep the X-Real-IP fallback and the '127.0.0.1' default and the exported signature unchanged (D-14 code half).

    2. Relocate `escapeRegExp` to `src/lib/utils.ts` as an exported function (regex body identical to the homepage-articles.ts original); `homepage-articles.ts` imports it from `@/lib/utils` and deletes its local copy.

    3. Apply at both remaining sites: `articles/route.ts` wraps the trimmed search keyword in `escapeRegExp(...)` before `new RegExp(..., 'i')`, and skips the regex filter entirely when the trimmed keyword exceeds 100 characters (graceful defense-in-depth — no 400, search just doesn't filter); `blacklist.ts` sweeper wraps its `rootDomain || hostname || targetDomainOrUrl` argument the same way.

    4. Create `tests/lib/client-ip.test.ts`: single XFF entry → that entry; multi-entry XFF → LAST entry returned even when the first entry is a spoofed value; no XFF but X-Real-IP present → X-Real-IP; neither → 127.0.0.1.

    5. Create `tests/lib/redos.test.ts`: an evil-pattern input (nested-quantifier shape) against the escapeRegExp-wrapped builder returns literally and a direct timed invocation of the search path completes well under a generous time-box (assert elapsed < 2000ms and correct literal matches); over-100-char keyword yields an unfiltered result set rather than an error.

    6. `DEPLOY.md` §8: replace the `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;` directive with `proxy_set_header X-Forwarded-For $remote_addr;` plus a short comment that the header is overwritten, not appended; add the manual VPS steps (edit `/etc/nginx/sites-available/websiteafiliate` in BOTH the 80 and 443 server blocks per §9, `nginx -t`, `systemctl reload nginx`) and the post-deploy verification curl from 01-VALIDATION.md (spoofed XFF header must not reach ClickLog.ip_address).
  </action>
  <verify>
    <automated>npx vitest run tests/lib/client-ip.test.ts tests/lib/redos.test.ts; if ($?) { Select-String -Path DEPLOY.md -Pattern "X-Forwarded-For \$remote_addr" -Quiet }</automated>
    <fails_when>non-zero exit from vitest, "failed" in the summary, or Select-String returning False (directive not documented)</fails_when>
  </verify>
  <done>Last-hop IP parsing live, escapeRegExp at every user-text→RegExp site with the length cap, DEPLOY.md carries the overwrite directive + VPS steps; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| open internet → public write endpoints | Unauthenticated bursts (subscribe / click / redirect) hit these routes directly |
| client-supplied XFF header → getClientIp | Attacker-controlled header previously decided the limiter key |
| public search query → RegExp engine | Unauthenticated input previously compiled as a raw regex |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-14 | DoS | subscribe/email bombing | high | mitigate | hard 429 at 5/60s per IP with Retry-After (Task 1) |
| T-1-15 | DoS | click/redirect flood inflating analytics | high | mitigate | 60/60s flood cap + 60s dedupe with silent skip, no 429 (Task 2) |
| T-1-16 | Spoofing | XFF first-hop forgery defeating limiter keying | high | mitigate | last-hop parse in getClientIp + Nginx overwrite directive + manual VPS step (Task 3, user_setup) |
| T-1-17 | DoS | ReDoS via public search | high | mitigate | escapeRegExp at both sites + 100-char cap + timed test (Task 3) |
| T-1-SC | Tampering | npm installs | high | mitigate | none — zero packages installed in this plan |
</threat_model>

<verification>
- `npx vitest run tests/api/rate-limit.test.ts tests/lib/client-ip.test.ts tests/lib/redos.test.ts` green
- `npx vitest run tests/api/insider-lifecycle.test.ts tests/api/tracking-redirect.test.ts` still green (no regressions from limiter + IP changes)
- `npm test` full suite green at wave merge
- Manual (VPS, post-deploy): apply the §8 directive; spoofed-XFF curl no longer pollutes ClickLog.ip_address (01-VALIDATION.md manual item)
</verification>

<success_criteria>
- Bursts of unauthenticated writes are rate-limited per policy (429 subscribe; silent-skip click/redirect) — ROADMAP success criterion 5, first clause
- A crafted ReDoS search pattern does not hang the server — ROADMAP success criterion 5, second clause
- Affiliate base_url scheme restriction ships in plan 03; combined, criterion 5 is fully met
</success_criteria>

<output>
Create `.planning/phases/01-security-remediation/01-05-SUMMARY.md` when done
</output>
