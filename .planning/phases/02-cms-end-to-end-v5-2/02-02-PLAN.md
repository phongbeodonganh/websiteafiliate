---
phase: 02-cms-end-to-end-v5-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/faq-jsonld.ts
  - src/lib/placement-order.ts
  - src/app/article/[slug]/page.tsx
  - tests/lib/faq-jsonld.test.ts
  - tests/lib/placement-order.test.ts
autonomous: true
requirements: [CMS-02, CMS-03]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A published article whose `faq_schema` holds at least one complete {question, answer} pair renders a third `<script type=\"application/ld+json\">` block whose object is `@type: 'FAQPage'` with `mainEntity` an array of `{ '@type': 'Question', name, acceptedAnswer: { '@type': 'Answer', text } }` — one entry per complete pair, in stored array order (CMS-02, C-1)"
    - "An article with no `faq_schema`, an empty array, or only incomplete pairs renders NO FAQPage script at all — the existing NewsArticle and BreadcrumbList blocks are unaffected (CMS-02 edge: empty)"
    - "FAQ row pairs whose question or answer is empty/whitespace-only are dropped from the FAQPage block silently, matching the editor's existing drop-incomplete rule (D-10) — a pair is kept only when both sides have non-whitespace content"
    - "Question and answer text is emitted through `serializeJsonLd` (the existing `<` → `\\u003c` escaper), so a question containing `</script>` cannot break out of the JSON-LD block"
    - "Affiliate placements render in canonical position order `top_cta` → `middle_comparison` → `footer_banner`, independent of the order they were stored in `affiliate_placements` (CMS-03, A2)"
    - "Placements sharing the same `position_label` keep their stored relative order (stable sort) — equal keys never reorder each other (CMS-03 edge: ordering)"
    - "An unknown/legacy position_label (e.g. `middle`) sorts after the three canonical labels but is never dropped, and never sorts before a canonical label"
    - "The first position in canonical order feeds the Editor's Verdict; the remaining placements render as CTAs in canonical order — the public page order is deterministic regardless of click order in the editor (CMS-03)"
  artifacts:
    - "src/lib/faq-jsonld.ts — NEW: `buildFaqPageSchema(faqSchema)` pure builder returning the FAQPage object or null"
    - "src/lib/placement-order.ts — NEW: `ORDERED_POSITION_LABELS`, `positionRank(label)`, `sortPlacementsByPosition(placements)` stable canonical-order helper"
    - "src/app/article/[slug]/page.tsx — MOD: sort placements via the helper; emit the FAQPage JSON-LD script when the builder returns non-null"
    - "tests/lib/faq-jsonld.test.ts — NEW: builder truth table + escaping"
    - "tests/lib/placement-order.test.ts — NEW: canonical order, stability, unknown-label behavior"
  key_links:
    - "doc.faq_schema ({question, answer}[]) → buildFaqPageSchema → serializeJsonLd → third <script type=\"application/ld+json\"> in ArticleDetailPage"
    - "doc.affiliate_placements (populated) → sortPlacementsByPosition → placements[0] is the Editor's Verdict, slice(1) are CTAs"
  assumptions:
    - "[CMS-02/unclassified|flagged-assumption] Manual review of the unclassified probe for CMS-02: the requirement's observable contract is the emitted JSON-LD object shape, resolved to the FAQPage Question/acceptedAnswer mapping above; the only adjacent surface (the editor's faq_rows state) is covered by plan 02-06."
    - "[CMS-03/empty|flagged-assumption] An article with zero populated placements renders neither an Editor's Verdict nor a placements aside — verified as already-true by the existing `populatedPlacements.filter(...)` and `verdictPlacement &&` guards; the sort must preserve this (empty in → empty out)."
    - "[CMS-03/adjacency|flagged-assumption] 'Adjacency' for placements means two placements with the same position_label: neither merges nor collides — both render, adjacent, in stored order (stable sort)."
    - "[CMS-03/ordering|flagged-assumption] Canonical order is the A2 recommendation (top_cta → middle_comparison → footer_banner); the ROADMAP success criterion says only 'render in order', so this plan pins the concrete order as the A2 assumption."
  prohibitions:
    - statement: "The FAQPage block must never be emitted with an `acceptedAnswer` whose `text` is non-string or empty — an incomplete pair must be dropped, never emitted as a half-empty Question"
      status: flagged-unverified
      verification: none
    - statement: "Article-rendered FAQ text must never be interpolated into the page as raw HTML — JSON-LD is the only output path (React text/JSON serialization only)"
      status: flagged-unverified
      verification: none
    - statement: "Affiliate placement ordering must not be driven by editor click order or by array index alone — the canonical position order must be the deterministic source"
      status: flagged-unverified
      verification: none
---

<objective>
Deliver the two public-side capabilities the ROADMAP success criteria #1 and #3 require but which do not exist today: FAQPage JSON-LD built from `faq_schema`, and deterministic canonical ordering of affiliate placements.

Purpose: CMS-02's stated end-to-end contract ("FAQ pairs auto-embed as FAQPage JSON-LD on the public page") is currently unmet — the article page emits only NewsArticle + BreadcrumbList (RESEARCH contradiction C-1). Success criterion #3 requires placements to "render in order", but the render path preserves click order, not position order (Pitfall 6).

Output: two pure, unit-tested builders plus their wiring into `src/app/article/[slug]/page.tsx`.
</objective>

<execution_context>
@C:/Users/PC/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/PC/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cms-end-to-end-v5-2/02-CONTEXT.md
@.planning/phases/02-cms-end-to-end-v5-2/02-RESEARCH.md

Framework contract (NOT training-data Next.js): `params` is a Promise — `await params` is already correct in this file; do not revert it. Read `node_modules/next/dist/docs/` before altering any page-level API.
</context>

## Artifacts this phase produces (plan 02)

- `src/lib/faq-jsonld.ts` — NEW: `buildFaqPageSchema(faqSchema: {question: string; answer: string}[] | undefined): object | null`
- `src/lib/placement-order.ts` — NEW: `ORDERED_POSITION_LABELS`, `positionRank(label: string): number`, `sortPlacementsByPosition<T extends { positionLabel: string }>(placements: T[]): T[]`
- `src/app/article/[slug]/page.tsx` — MOD: import + apply both helpers; add the FAQPage `<script>`
- `tests/lib/faq-jsonld.test.ts` — NEW
- `tests/lib/placement-order.test.ts` — NEW

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — FAQ Q&A pair → FAQPage JSON-LD on a rendered article</name>
  <files>src/lib/faq-jsonld.ts, src/app/article/[slug]/page.tsx, tests/lib/faq-jsonld.test.ts</files>
  <read_first>
    - src/app/article/[slug]/page.tsx — whole file: the two existing JSON-LD blocks (articleSchema ~149-157, breadcrumbSchema ~167-176), the `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: serializeJsonLd(...) }} />` emission pair at ~181-182, the placement assembly at ~96-109, and how `doc.faq_schema` is currently ignored
    - src/lib/seo.ts — `serializeJsonLd` (~line 81: `JSON.stringify(value).replace(/</g, '\\u003c')`) — the only permitted serialization path
    - src/lib/db/models.ts — `faq_schema?: { question: string; answer: string }[]` on `IArticle` (the stored shape)
    - src/app/admin/page.tsx — the editor's existing filter `faqRows.filter((f) => f.question.trim() && f.answer.trim())` (~line 1856) — mirror this drop-incomplete rule exactly
    - .planning/phases/02-cms-end-to-end-v5-2/02-RESEARCH.md — section "C-1: FAQPage JSON-LD is absent from the public article page" (the exact emission point and the mainEntity shape)
    - tests/lib/sanitize.test.ts — the `tests/lib/*` pure-function test conventions (import style, describe/it shape)
  </read_first>
  <action>
    Work test-first on the builder, then wire it for keeps.

    1. RED: create `tests/lib/faq-jsonld.test.ts` asserting `buildFaqPageSchema`:
       - two complete pairs → object with `'@context': 'https://schema.org'`, `'@type': 'FAQPage'`, `mainEntity` length 2, each `{ '@type': 'Question', name: <question>, acceptedAnswer: { '@type': 'Answer', text: <answer> } }` in stored order.
       - `undefined`, `[]`, `[{question:'',answer:'a'}]`, `[{question:'q',answer:'   '}]` → `null`.
       - mixed list → only complete pairs in the output; length reflects the filtered count.
       - a pair whose question contains the literal markup-closing sequence for a script tag → `serializeJsonLd(buildFaqPageSchema(...))` output contains no raw `<` character (assert the serialized string does not contain '<').
       - non-string `question`/`answer` values (defensive) → dropped, not emitted.
       Confirm RED before implementing.

    2. Implement `src/lib/faq-jsonld.ts`: export a pure `buildFaqPageSchema` that accepts the optional array, returns `null` when the filtered list is empty, otherwise the FAQPage object. Filter with the same rule as the editor: both `question` and `answer` must be strings with non-whitespace content; trim the emitted values. No DB access, no Next imports — this is a pure builder. Single-quote style, named export, no barrel file (CONVENTIONS.md).

    3. Wire into `src/app/article/[slug]/page.tsx`: compute `const faqPageSchema = buildFaqPageSchema(doc.faq_schema);` next to the other schema objects, and conditionally emit a third `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: serializeJsonLd(faqPageSchema) }} />` immediately after the breadcrumb script, guarded by `faqPageSchema &&`. Change nothing else about the two existing blocks.

    Do not add any other public-side UI. The visible FAQ section, if any, is out of this plan's scope; only the structured-data render is required.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/faq-jsonld.test.ts</automated>
    <fails_when>non-zero exit code, or "failed" appearing in the vitest summary line, or "0 passed" in the summary</fails_when>
  </verify>
  <done>A published article with complete FAQ pairs emits a valid FAQPage JSON-LD block through the shared escaper; articles without complete pairs emit none; the builder is pure and unit-tested (CMS-02, C-1).</done>
  <acceptance_criteria>
    - `src/lib/faq-jsonld.ts` exports `buildFaqPageSchema` and imports nothing from `next/*` or the DB layer
    - `buildFaqPageSchema([{question:'q',answer:'a'}])` returns `{ '@type': 'FAQPage', mainEntity: [ { '@type': 'Question', name: 'q', acceptedAnswer: { '@type': 'Answer', text: 'a' } } ] }`
    - `buildFaqPageSchema(undefined)`, `buildFaqPageSchema([])`, and a list of only incomplete pairs each return `null`
    - The `serializeJsonLd` output for any built schema contains no raw `<` character
    - `src/app/article/[slug]/page.tsx` contains a third `application/ld+json` script emitted only when the builder returns non-null
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Expansion — canonical affiliate-placement render order</name>
  <files>src/lib/placement-order.ts, src/app/article/[slug]/page.tsx, tests/lib/placement-order.test.ts</files>
  <read_first>
    - src/app/article/[slug]/page.tsx — the placement assembly and the verdict/remaining split (~96-109): `verdictPlacement = placements[0]`, `remainingPlacements = placements.slice(1)`
    - src/components/AffiliateCtaBlock.tsx — `POSITION_LABELS` map (~18-23) showing the live labels: `top_cta`, `middle`, `middle_comparison`, `footer_banner`
    - src/lib/db/models.ts — `affiliate_placements?: { affiliate_link_id; position_label }[]` with `position_label` default `'top_cta'`
    - src/app/admin/page.tsx — `togglePlacement` (~1942-1955) appending on toggle — the source of click-order drift this task neutralizes
    - .planning/phases/02-cms-end-to-end-v5-2/02-RESEARCH.md — "Pitfall 6: Affiliate placement render order is not position-ordered" (A2)
    - .planning/codebase/CONVENTIONS.md — kebab-case lib file naming
  </read_first>
  <action>
    1. Create `src/lib/placement-order.ts` exporting:
       - `ORDERED_POSITION_LABELS = ['top_cta', 'middle_comparison', 'footer_banner'] as const;`
       - `positionRank(label: string): number` — index in `ORDERED_POSITION_LABELS`; any label not in the list (including legacy `middle`) returns `ORDERED_POSITION_LABELS.length` so all unknown labels tie after the canonical three.
       - `sortPlacementsByPosition<T extends { positionLabel: string }>(placements: T[]): T[]` — a **stable** sort by `positionRank(positionLabel)`. Use a decorate-sort-undecorate (attach original index as the tiebreaker) rather than relying on `Array.prototype.sort` stability, so the guarantee is explicit and testable. Return a new array; never mutate the input.

    2. Create `tests/lib/placement-order.test.ts` asserting:
       - `[footer_banner, top_cta, middle_comparison]` → `[top_cta, middle_comparison, footer_banner]`.
       - two placements with the same `position_label` keep their input order.
       - a legacy `middle` placement sorts after all three canonical labels and is present in the output (not dropped).
       - empty array → empty array; single element → unchanged.
       - input array is not mutated (assert the original order after the call).

    3. In `src/app/article/[slug]/page.tsx`, apply the helper to the assembled `placements` list before the verdict/remaining split, so `placements[0]` is the highest-ranked position and `slice(1)` follows canonical order. Keep the existing `.filter((placement) => placement.affiliate_link_id?._id)` guard before sorting. No change to `EditorVerdict`, `AffiliateCtaBlock`, or the `StickyMobileBar` wiring beyond the order they receive.

    Do not reorder the array at write time in the CMS (that is plan 02-06's payload concern) — render-side ordering is the deterministic source of truth here.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/placement-order.test.ts tests/lib/faq-jsonld.test.ts; if ($?) { npx tsc --noEmit }</automated>
    <fails_when>non-zero exit, "failed" in the vitest summary line, or any tsc error reported</fails_when>
  </verify>
  <done>Placements render in canonical top_cta → middle_comparison → footer_banner order regardless of stored/click order, stable for ties, tolerant of unknown labels, and typecheck-clean (CMS-03, A2).</done>
  <acceptance_criteria>
    - `src/lib/placement-order.ts` exports `ORDERED_POSITION_LABELS`, `positionRank`, and `sortPlacementsByPosition`
    - `sortPlacementsByPosition([{positionLabel:'footer_banner'},{positionLabel:'top_cta'}])` returns `top_cta` first
    - Two entries sharing a `positionLabel` retain their input relative order (stable)
    - An unknown label (`middle`) is present in the output after all canonical labels
    - The input array is unchanged after the call (no mutation)
    - `src/app/article/[slug]/page.tsx` applies the sort before computing `verdictPlacement`/`remainingPlacements`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored article document → public SSR JSON-LD | Article and FAQ text authored by CMS users becomes machine-readable output on every page view |
| stored placement array → public render | Affiliate link metadata and position labels drive CTA ordering and the redirect targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | FAQ text breaking out of the JSON-LD script tag (stored XSS) | high | mitigate | all output through `serializeJsonLd` (`<` → `\u003c`); builder never emits raw HTML; assertion that serialized output contains no `<` (Task 1) |
| T-02-07 | Information disclosure | malformed FAQ pairs leaking into structured data | low | mitigate | incomplete pairs dropped by the same rule the editor uses (Task 1) |
| T-02-08 | Tampering | placement order manipulated to promote an arbitrary offer | low | accept | order is cosmetic and already gated by admin-only placement management (AUTH-03); canonical sort reduces manipulation surface |
| T-02-SC | Tampering | npm installs | high | mitigate | none — this plan installs zero packages (Research: zero-new-packages phase) |
</threat_model>

<verification>
- `npx vitest run tests/lib/faq-jsonld.test.ts tests/lib/placement-order.test.ts` green
- `npx tsc --noEmit` clean
- Manual spot check (optional): load a published article in devtools and confirm three ld+json blocks (NewsArticle, BreadcrumbList, FAQPage) and placements in canonical order
</verification>

<success_criteria>
- A published article with FAQ pairs emits FAQPage JSON-LD structurally (ROADMAP success criterion 1, CMS-02, C-1)
- Placements render in canonical position order on the public page (ROADMAP success criterion 3, CMS-03, A2)
- Both capabilities are pure functions with unit tests; no new packages, no public UI redesign
</success_criteria>

<output>
Create `.planning/phases/02-cms-end-to-end-v5-2/02-02-SUMMARY.md` when done
</output>
