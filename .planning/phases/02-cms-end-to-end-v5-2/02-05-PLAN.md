---
phase: 02-cms-end-to-end-v5-2
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-03]
files_modified:
  - src/app/api/v1/cms/users/[id]/route.ts
  - src/app/admin/page.tsx
  - tests/api/cms-users-update.test.ts
  - tests/api/cms-rbac-affiliate-403.test.ts
  - tests/api/articles-list-scoping.test.ts
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-04]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An admin can update a user's `role`, `status`, `name`, and `avatar` and the change persists to the users collection (AUTH-04, D-14)"
    - "`PUT /api/v1/cms/users/:id` does NOT accept or apply a `password` field — a request carrying `password` leaves `password_hash` unchanged (D-14; password recovery stays CLI-only per Phase 1 D-04)"
    - "`PUT /api/v1/cms/users/:id` returns the updated `avatar` in its response data (today it is omitted from the select)"
    - "A non-admin (editor/author) token receives 403 from `POST`/`PUT`/`DELETE` on `/api/v1/cms/affiliate-links*` — affiliate-link management is admin-only (AUTH-03)"
    - "A non-admin (editor/author) token can still `GET /api/v1/cms/affiliate-links` — AUTH-03 hides management but still permits selecting an existing link to attach to an article"
    - "A non-admin's `GET /api/v1/cms/articles` returns only articles whose `author_id` is their own — never another author's article (AUTH-02)"
    - "An admin's `GET /api/v1/cms/articles` returns articles authored by anyone (AUTH-02)"
    - "Admin-only tabs (Affiliate Campaigns, Team & Creators, Categories & Sub-Cats, Link & Blacklist Center, Global SEO & System, SEO Insights, Insider) are absent from an editor/author's sidebar nav (D-13)"
    - "An editor/author who reaches an admin-only tab via URL sees `You don't have access to this section.` with a link back to Articles — never the 'Under Construction…' placeholder and never another user's data (D-13, UI-SPEC copy row 'RBAC — hidden tab')"
    - "Calling an admin-only CMS route with an editor/author token still returns 403 (defense in depth) — the UI hiding does not replace route enforcement (D-13)"
  artifacts:
    - "src/app/api/v1/cms/users/[id]/route.ts — MOD: PUT destructures `avatar`, applies it; `password` removed from the destructure and from any assignment"
    - "src/app/admin/page.tsx — MOD: edit-user modal (role/status/name/avatar), wired Edit button, permission-denied fallback in `renderContent`"
    - "tests/api/cms-users-update.test.ts — NEW: role/status/name/avatar persist; password ignored; 403 non-admin"
    - "tests/api/cms-rbac-affiliate-403.test.ts — NEW: editor/author 403 on affiliate-link CRUD; admin allowed"
    - "tests/api/articles-list-scoping.test.ts — NEW: non-admin list scoped to author_id; admin sees all"
  key_links:
    - "admin Edit button → edit-user modal → `PUT /api/v1/cms/users/:id` → role/status/name/avatar persisted"
    - "editor/author session → nav render (admin block gated) → no admin tab; URL entry → 403-style permission panel"
    - "editor/author session → affiliate-links routes → 403 (unchanged route guard, now test-pinned)"
  assumptions:
    - "[AUTH-04/adjacency|flagged-assumption] A user may hold at most one role from the enum (admin/editor/author); status is exactly `'active'` or `'inactive'`. No multi-role or additive-status model is introduced."
    - "[AUTH-04/empty|flagged-assumption] `avatar` is a free string (matches the model and the existing first-letter fallback); an empty/omitted avatar is legitimate and the row falls back to the first letter of name or username."
    - "[AUTH-04/encoding|flagged-assumption] Role/status/name values are compared as exact strings after trim; no normalization/casing contract is introduced beyond the existing model enum validation."
    - "[AUTH-02/ordering|flagged-assumption] The scoped list is ordered by `created_at DESC` for both admin and non-admin — the existing sort is preserved and no per-role ordering difference is introduced."
    - "[AUTH-02/empty|flagged-assumption] A non-admin with zero articles receives `{ status:'success', data: [] }` — an empty list, never a 403 or a leak of the global count."
  prohibitions:
    - statement: "The CMS must never expose a password set/reset/change control for an existing user — password recovery stays CLI-only"
      status: flagged-unverified
      verification: none
    - statement: "Hiding an admin tab in the UI must never be treated as the access control — every backing route must keep returning 403 for non-admins"
      status: flagged-unverified
      verification: none
    - statement: "A non-admin must never be able to read, list, edit, or delete another author's article through any CMS route"
      status: flagged-unverified
      verification: none
---

<objective>
Complete user management (role/status/name/avatar, no password path), add the missing edit-user modal, and pin the full RBAC surface with regression tests — article ownership scoping, affiliate-link 403, and admin-tab hiding with a permission-denied fallback.

Purpose: AUTH-04's API exists but `avatar` is not updatable and the Edit button has no handler (RESEARCH AUTH-04 gap); AUTH-02/AUTH-03 are enforced but untested (research: no dedicated affiliate-403 or list-scoping test). This plan makes the RBAC contract observable and keeps the route enforcement primary over UI hiding (D-13/D-14/D-16).

Output: a fixed users PUT route, an edit-user modal + permission fallback in the admin shell, and three new regression suites.
</objective>

<execution_context>
@C:/Users/PC/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/PC/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cms-end-to-end-v5-2/02-CONTEXT.md
@.planning/phases/02-cms-end-to-end-v5-2/02-RESEARCH.md
@.planning/phases/02-cms-end-to-end-v5-2/02-UI-SPEC.md

Depends on plan 02-01 (async `getAuthUser` — write `await getAuthUser(req)` in touched routes/handlers) and plan 02-03 (shared `cmsFetch` helper — Task 1 uses it). This plan also edits `src/app/admin/page.tsx`, so it follows 02-03 in that file's edit sequence. The wave-merge suite validates the final form.
Framework contract (NOT training-data Next.js): do not restructure the admin URL-state contract; targeted edits only (HYG-02 owns modularization).
</context>

## Artifacts this phase produces (plan 05)

- `src/app/api/v1/cms/users/[id]/route.ts` — MOD: avatar updatable, password path removed
- `src/app/admin/page.tsx` — MOD: edit-user modal + handler, permission-denied fallback
- `tests/api/cms-users-update.test.ts`, `tests/api/cms-rbac-affiliate-403.test.ts`, `tests/api/articles-list-scoping.test.ts` — NEW

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — admin edits a team member's role/status/name/avatar end-to-end</name>
  <files>src/app/api/v1/cms/users/[id]/route.ts, src/app/admin/page.tsx, tests/api/cms-users-update.test.ts</files>
  <read_first>
    - src/app/api/v1/cms/users/[id]/route.ts — whole file: PUT destructures only `{ name, role, status, password }` (~line 18) and applies `password` (~line 30); the response (~34-43) omits `avatar`
    - src/app/api/v1/cms/users/route.ts — GET maps `avatar`; POST derives the first-letter fallback (`name[0]`/`username[0]`)
    - src/app/admin/page.tsx — `UsersView` (~1409-1558): the Edit button with no handler (~1477-1479), the add-user modal pattern to mirror (~1487-1556), `handleAddUser` (~748-780), `loadUsersData` (~695-700), and the `usersList` row fields (avatar/role/status)
    - src/lib/cms-fetch.ts — the shared helper from plan 02-03 (use it for the save call if present)
    - .planning/phases/02-cms-end-to-end-v5-2/02-UI-SPEC.md — Copywriting rows for user management (edit modal title, field labels, password note, save success, deactivation warning, inactive row status) and UI Considerations E4
    - tests/api/articles-ownership.test.ts — seeding/signing conventions for the users test
  </read_first>
  <action>
    1. Fix `src/app/api/v1/cms/users/[id]/route.ts` PUT: destructure `{ name, role, status, avatar }` and drop `password` entirely from the destructure and from any assignment. Apply `avatar` when provided (`if (avatar !== undefined) userDoc.avatar = avatar;`). Include `avatar` in the success response data. Keep the admin guard returning 403 for a missing/non-admin principal and the 404 for a missing user. Do not change DELETE.

    2. Add an edit-user modal to `UsersView` in `src/app/admin/page.tsx`: wire the existing Edit button (~1477) to open it with the row's current values. Fields are `Name` / `Role` (admin/editor/author) / `Status` (active/inactive) / `Avatar` — an informational password note `Passwords are managed outside the CMS. Use the reset-admin CLI to recover an account.` replaces any password control. Modal title `Edit team member`, submit saves via `PUT /api/v1/cms/users/:id`, success notice `Team member updated.`, then `loadUsersData()`. When status is set to `inactive`, show the inline note `Inactive users are locked out immediately — their existing sessions stop working on the next request.` before saving (no extra confirm — the action is reversible). Style with the existing dark-shell tokens and `LuxuryButton`; new labels use the sentence-case 12px/800 label role and the 4 declared sizes only.

    3. Create `tests/api/cms-users-update.test.ts`: seed an admin and a target user; `PUT` with `{ name, role, status, avatar }` and assert 200 + all four fields persisted and returned; `PUT` with a `password` field only and assert `password_hash` is unchanged in the DB and the response has no password; `PUT` with a non-admin token → 403; `PUT` for a missing id → 404.

    Manual UAT (record in summary): open the users tab, click Edit on a row, change role/status/name/avatar, save, and confirm the row reflects the change without a page reload.
  </action>
  <verify>
    <automated>npx vitest run tests/api/cms-users-update.test.ts</automated>
    <fails_when>non-zero exit code, "failed" in the vitest summary line, "0 passed" in the summary, or a `password_hash` change observed after a password-only PUT</fails_when>
  </verify>
  <done>An admin edits role/status/name/avatar through the CMS and the change persists; the password path is gone entirely; `GET`/`PUT` both carry `avatar` (AUTH-04, D-14).</done>
  <acceptance_criteria>
    - `PUT /api/v1/cms/users/:id` destructures `avatar` and applies it; a returned success body includes `avatar`
    - `password` does not appear in the PUT destructure or any assignment; a password-only PUT leaves `password_hash` unchanged in the DB
    - The edit-user modal exists with `Name`/`Role`/`Status`/`Avatar` fields, the password note, the deactivation warning, and the `Edit team member` title
    - A non-admin PUT returns 403; a PUT for a missing id returns 404
    - `npx vitest run tests/api/cms-users-update.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: RBAC regression tests — affiliate 403 + article list scoping (AUTH-02, AUTH-03)</name>
  <files>tests/api/cms-rbac-affiliate-403.test.ts, tests/api/articles-list-scoping.test.ts</files>
  <read_first>
    - src/app/api/v1/cms/affiliate-links/route.ts — GET is any-authenticated (401 only); POST is admin-only 403 (~49-54)
    - src/app/api/v1/cms/affiliate-links/[id]/route.ts — PUT/DELETE admin-only 403 (~13-18)
    - src/app/api/v1/cms/articles/route.ts — GET filter `user.role === 'admin' ? {} : { author_id: user.userId }` (~18)
    - tests/api/articles-ownership.test.ts — the existing PUT/DELETE ownership suite this new file complements (do not duplicate it)
    - tests/api/cms-auth-401.test.ts — how editor/author tokens are signed and routes invoked
    - .planning/phases/02-cms-end-to-end-v5-2/02-RESEARCH.md — AUTH-02/AUTH-03 test-map rows (list path + affiliate 403 are the Wave 0 gaps)
  </read_first>
  <action>
    1. Create `tests/api/cms-rbac-affiliate-403.test.ts`: seed an admin, an editor, an author, and one affiliate link. For editor and author tokens, assert 403 on `POST /api/v1/cms/affiliate-links`, `PUT /api/v1/cms/affiliate-links/:id`, and `DELETE /api/v1/cms/affiliate-links/:id`; assert the link document is unchanged after the rejected writes. Assert the admin token is allowed on the same verbs (201/200) — proving the gate is role-based, not blanket. Also assert an editor/author token CAN still `GET /api/v1/cms/affiliate-links` (200, the existing list) — AUTH-03 permits selecting an existing link to attach; only management is forbidden.

    2. Create `tests/api/articles-list-scoping.test.ts`: seed two authors each owning one article plus an admin. `GET /api/v1/cms/articles` with author A's token returns exactly A's article (assert the returned ids); with author B's token returns exactly B's article; with the admin token returns both. Assert a non-admin with zero articles gets `status:'success'` and an empty `data` array (not 403). Assert the response never includes the other author's article title.

    3. Do not modify production route code in this task — these tests pin existing enforcement. If any assertion fails, treat it as a real RBAC defect and fix the route (do not weaken the test).
  </action>
  <verify>
    <automated>npx vitest run tests/api/cms-rbac-affiliate-403.test.ts tests/api/articles-list-scoping.test.ts tests/api/articles-ownership.test.ts</automated>
    <fails_when>non-zero exit code, "failed" in the vitest summary line, or a status other than 403 observed for a non-admin affiliate-link write</fails_when>
  </verify>
  <done>Affiliate-link CRUD is proven admin-only (403 for editor/author), and the article list is proven scoped to `author_id` for non-admins with an empty-list (not 403) result for a zero-article author (AUTH-02, AUTH-03, D-16).</done>
  <acceptance_criteria>
    - `tests/api/cms-rbac-affiliate-403.test.ts` asserts 403 for editor and author tokens on POST/PUT/DELETE of affiliate links, and success for the admin token
    - The same test asserts a 200 for editor/author `GET /api/v1/cms/affiliate-links` (select-to-attach remains permitted)
    - The affiliate link document is unchanged after each rejected write
    - `tests/api/articles-list-scoping.test.ts` asserts author A's list contains only A's article, B's only B's, admin's both
    - A non-admin with zero articles gets `{ status:'success', data: [] }`
    - `npx vitest run tests/api/cms-rbac-affiliate-403.test.ts tests/api/articles-list-scoping.test.ts tests/api/articles-ownership.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Admin-tab hiding + permission-denied fallback (D-13)</name>
  <files>src/app/admin/page.tsx, tests/api/cms-rbac-affiliate-403.test.ts</files>
  <read_first>
    - src/app/admin/page.tsx — the sidebar nav block (~3896-3913, admin items already wrapped in `currentUser.role === 'admin' &&`), `NavItem` (~3860-3874, `requiredRole` rule), `renderContent` (~3843-3858, the 'Under Construction...' fallback at 3857), and the tab-gating useEffects (~718-728)
    - .planning/phases/02-cms-end-to-end-v5-2/02-UI-SPEC.md — Interaction Contract 8, UI Considerations E7, copy row 'RBAC — hidden tab'
    - src/app/admin/page.tsx — `activeTab`/`navigate` so the fallback does not bypass URL state
  </read_first>
  <action>
    1. In `renderContent`, replace the generic `Under Construction...` fallback for a *known admin-only tab* reached by a non-admin with the permission contract: `You don't have access to this section.` plus a link/button back to Articles (`Access denied` styling: rose/slate, not amber). Guard by checking the requested `activeTab` against the admin-only set (insights, subscribers, categories, users, links, blacklist, settings) and `currentUser.role !== 'admin'`. Keep the `Under Construction...` copy only for genuinely unknown tabs.

    2. Confirm the sidebar admin block stays gated so editor/author accounts see only Dashboard + Article Management; if any admin `NavItem` sits outside the `currentUser.role === 'admin'` wrapper, move it inside. `NavItem`'s `requiredRole` check must still return null for a mismatched role.

    3. Add a source-contract assertion (in `tests/api/cms-rbac-affiliate-403.test.ts` or a small addition to it) that reads `src/app/admin/page.tsx` and asserts the admin-only tab ids appear inside the role-gated nav region and that the permission copy string `You don't have access to this section.` is present in `renderContent`'s fallback — pinning that a future edit cannot silently drop the fallback.

    4. Defense in depth is unchanged: the backing routes keep their 403 (proven by Task 2). This task is the UI half only.

    Manual UAT (record in summary): log in as an editor and confirm no admin tabs appear and that navigating to `?tab=users` shows the access-denied panel, not another user's data.
  </action>
  <verify>
    <automated>npx vitest run tests/api/cms-rbac-affiliate-403.test.ts tests/api/cms-auth-401.test.ts; if ($?) { npx tsc --noEmit }</automated>
    <fails_when>non-zero exit, "failed" in the vitest summary line, or any tsc error reported</fails_when>
  </verify>
  <done>Editor/author accounts see only their permitted tabs and a clear access-denied panel on a hidden tab; admin tabs remain visible to admins; backing routes still 403 (D-13).</done>
  <acceptance_criteria>
    - `renderContent` shows `You don't have access to this section.` with a link back to Articles for a non-admin on an admin-only tab id
    - The `Under Construction...` copy remains only for unknown tabs
    - Every admin-only `NavItem` sits inside the `currentUser.role === 'admin'` gate and `NavItem` returns null for a mismatched `requiredRole`
    - A source-contract assertion in the test file reads `src/app/admin/page.tsx` and confirms the permission copy string is present
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor/author session → admin-only CMS routes | UI hiding is cosmetic; route 403 is the real control |
| author session → article collection | Ownership scoping prevents cross-author read/write |
| admin session → user records | Role/status changes take effect on the target's live session (plan 02-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Elevation of Privilege | editor/author managing affiliate links | high | mitigate | admin-only 403 on POST/PUT/DELETE, pinned by regression tests (Task 2) |
| T-02-19 | Information disclosure | cross-author article visibility | high | mitigate | `author_id` list filter + item 403s, pinned by list-scoping tests (Task 2) |
| T-02-20 | Elevation of Privilege | admin tab reachable by URL for a non-admin | medium | mitigate | permission-denied fallback in `renderContent` + route 403 (Task 3) |
| T-02-21 | Repudiation | password changed through the CMS, bypassing CLI-only recovery | high | mitigate | PUT no longer accepts `password`; test asserts `password_hash` unchanged (Task 1) |
| T-02-SC | Tampering | npm installs | high | mitigate | none — this plan installs zero packages (Research: zero-new-packages phase) |
</threat_model>

<verification>
- `npx vitest run tests/api/cms-users-update.test.ts tests/api/cms-rbac-affiliate-403.test.ts tests/api/articles-list-scoping.test.ts` green
- `npx tsc --noEmit` clean
- Manual UAT: edit a user's role/status/name/avatar; log in as an editor to confirm hidden tabs + access-denied panel
</verification>

<success_criteria>
- Admin manages role/status/name/avatar with no password path (AUTH-04, D-14, ROADMAP criterion 4)
- Editor/author sees only own articles, gets 403 on affiliate management, and cannot reach admin tabs (AUTH-02/AUTH-03, ROADMAP criterion 3)
- Every RBAC claim is pinned by an automated regression test (D-16)
</success_criteria>

<output>
Create `.planning/phases/02-cms-end-to-end-v5-2/02-05-SUMMARY.md` when done
</output>
