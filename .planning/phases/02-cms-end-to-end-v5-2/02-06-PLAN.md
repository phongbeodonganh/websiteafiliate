---
phase: 02-cms-end-to-end-v5-2
plan: 06
type: execute
wave: 4
depends_on: [02-03, 02-04, 02-05]
files_modified:
  - scripts/seed-taxonomy.ts
  - src/lib/seed-taxonomy.ts
  - src/app/admin/page.tsx
  - tests/lib/seed-taxonomy.test.ts
  - tests/api/categories-taxonomy.test.ts
autonomous: true
requirements: [CMS-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `npx tsx scripts/seed-taxonomy.ts` twice leaves the taxonomy identical: the second run creates nothing and reports zero new rows — the seed is idempotent, upserting by slug and never calling `deleteMany` (D-11, A3)"
    - "After a run, the `categories` collection holds exactly the six V5.2 §1.2 top-level categories — `AI Use Cases` (`ai-use-cases`), `AI Content & Copywriting` (`ai-content-copywriting`), `AI Video & Image Generation` (`ai-video-image-generation`), `AI Automation & Agents` (`ai-automation-agents`), `AI Marketing & Sales` (`ai-marketing-sales`), `AI Audio & Code` (`ai-audio-code`)"
    - "`AI Use Cases` holds exactly five sub-categories, each `category_id`-linked to it: `AI for Creators & Media` (`ai-for-creators-media`), `AI for Real Estate & Sales` (`ai-for-real-estate-sales`), `AI for E-commerce & Business` (`ai-for-e-commerce-online-business`), `AI for Marketers & Agencies` (`ai-for-marketers-agencies`), `AI for Finance & Legal` (`ai-for-finance-legal-consulting`)"
    - "The seed coexists with pre-existing ad-hoc categories — it never deletes or renames an existing category or sub-category, only adds the missing spec nodes (idempotent upsert semantics)"
    - "The seeded taxonomy is served by the existing `GET /api/v1/cms/categories` shape (id, name, slug, description, subCategories[]) so the CMS CategoriesView renders the two-level tree with no route change (CMS-04)"
    - "In the article editor, a top-level category is required — saving with no category selected is blocked with the inline copy `Choose a primary category.` and the `status` must still be set (D-12, D-09)"
    - "The sub-category select is filtered to the chosen category's children and reset to empty whenever the parent category changes; a category with no children hides the sub-select entirely (D-12)"
    - "GEO fields (`focus_keyword`, `key_takeaways`, `entities`, `faq_schema`) remain optional — no client-side rule blocks a save because a GEO field is empty (D-09)"
    - "The FAQ row editor exposes add/remove repeating `{question, answer}` rows with the UI-SPEC copy and an accessible remove control (`title` + `aria-label`); incomplete rows are dropped from the `faqSchema` payload silently on save (D-10)"
  artifacts:
    - "src/lib/seed-taxonomy.ts — NEW: `SEED_TAXONOMY` constant (categories + sub-categories with slugs) and `seedTaxonomy()` idempotent upsert function returning `{ createdCategories, createdSubCategories }`"
    - "scripts/seed-taxonomy.ts — NEW: CLI entry (connect, run seedTaxonomy, log counts, exit); `npx tsx` per the scripts/ precedent"
    - "src/app/admin/page.tsx — MOD: category-required validation copy, sub-category reset/filter confirmation, FAQ row editor copy + accessibility"
    - "tests/lib/seed-taxonomy.test.ts — NEW: idempotency + exact tree + coexistence"
    - "tests/api/categories-taxonomy.test.ts — NEW: taxonomy GET shape over the seeded tree"
  key_links:
    - "scripts/seed-taxonomy.ts → connectToDatabase → seedTaxonomy() → CategoryModel/SubCategoryModel upsert by slug"
    - "seeded categories → GET /api/v1/cms/categories → CategoriesView tree → editor Level-1 select → Level-2 filtered select"
    - "editor faqRows state → filter incomplete → `faqSchema` payload → (plan 02-02) FAQPage JSON-LD"
  assumptions:
    - "[CMS-04/empty|flagged-assumption] An empty taxonomy is legitimate before the first seed run: the editor's Level-1 select shows the `-- Select Category --` placeholder and save is blocked only when the user attempts to save without choosing. The seed is a data task, not a guard."
    - "[CMS-04/ordering|flagged-assumption] Category and sub-category lists are served in `created_at DESC` order by the existing route; the seed does not impose a display order beyond creation sequence, and no client reordering is introduced."
    - "[CMS-04/adjacency|flagged-assumption] A sub-category belongs to exactly one parent category (`category_id` is singular). Changing a child's parent is not introduced in this phase."
    - "[A3|flagged-assumption] The seed is extracted to a callable `seedTaxonomy()` in `src/lib/` so idempotency is unit-testable, with `scripts/seed-taxonomy.ts` as the CLI wrapper — the existing `scripts/seed-*.ts` precedent requires the script, the testability requirement adds the lib function."
  prohibitions:
    - statement: "The taxonomy seed must never delete or overwrite existing taxonomy data — an idempotent upsert must not destroy categories or sub-categories an admin created"
      status: flagged-unverified
      verification: none
    - statement: "The seed script must never log or echo a connection string or credential — no secret material in stdout"
      status: flagged-unverified
      verification: none
    - statement: "No GEO field may be made required to save an article — optionality per D-09 must not be silently reversed by editor validation"
      status: flagged-unverified
      verification: none
---

<objective>
Give the CMS a working two-level taxonomy on day one by seeding the exact V5.2 §1.2 tree idempotently, and align the article editor's category/FAQ/GEO controls to the UI-SPEC contract (required Level-1 category, filtered optional Level-2, accessible FAQ row editor, GEO fields left optional).

Purpose: CMS-04's CRUD exists but the spec taxonomy is not seeded (D-11), and D-12/D-10's editor behavior needs the UI-SPEC copy and accessibility applied. This makes the taxonomy usable immediately and the editor pass the UI contract.

Output: a testable `seedTaxonomy()` + CLI script, editor validation/UX alignment, and two new test files.
</objective>

<execution_context>
@C:/Users/PC/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/PC/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cms-end-to-end-v5-2/02-CONTEXT.md
@.planning/phases/02-cms-end-to-end-v5-2/02-RESEARCH.md
@.planning/phases/02-cms-end-to-end-v5-2/02-UI-SPEC.md

Depends on plan 02-03 (shared `cmsFetch` helper + editor wiring), plan 02-04 (admin-page blacklist UI edits), and plan 02-05 (admin-page permission/tab changes) — this plan edits the same admin shell last and must build on their state.
Framework contract (NOT training-data Next.js): admin page is a client component; targeted edits only (HYG-02 owns modularization). Never import `src/lib/db/schema.ts` (dead twin).
</context>

## Artifacts this phase produces (plan 06)

- `src/lib/seed-taxonomy.ts` — NEW: `SEED_TAXONOMY`, `seedTaxonomy()`
- `scripts/seed-taxonomy.ts` — NEW: CLI entry
- `src/app/admin/page.tsx` — MOD: category-required copy, sub-category reset/filter, FAQ row editor copy + a11y
- `tests/lib/seed-taxonomy.test.ts`, `tests/api/categories-taxonomy.test.ts` — NEW

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — seed the V5.2 taxonomy once, idempotently, and see it in the CMS</name>
  <files>src/lib/seed-taxonomy.ts, scripts/seed-taxonomy.ts, tests/lib/seed-taxonomy.test.ts, tests/api/categories-taxonomy.test.ts</files>
  <read_first>
    - scripts/seed-affiliate-links-and-reviews.ts — the `upsertCategory`/`upsertSubCategory` find-by-slug pattern (11-21) and the `main()` + `connectToDatabase()` CLI shape to copy
    - src/lib/db/models.ts — `ICategorySchema`/`ISubCategorySchema` (fields: name, slug, description, category_id, meta_title, meta_description, created_at)
    - src/app/api/v1/cms/categories/route.ts — the GET shape (id, name, slug, description, metaTitle, metaDescription, createdAt, subCategories[]) the seed's data must satisfy
    - specv2.md §1.2 — the exact category names/slugs and the five `AI Use Cases` sub-categories (authoritative)
    - .planning/codebase/CONVENTIONS.md — scripts conventions; the lazy env getter pattern
    - tests/api/articles-ownership.test.ts — seeding conventions for the Mongo-backed taxonomy test
    - .planning/phases/02-cms-end-to-end-v5-2/02-RESEARCH.md — D-11 and "Runtime State Inventory" (seed must coexist with existing categories)
  </read_first>
  <action>
    1. Create `src/lib/seed-taxonomy.ts` exporting:
       - `SEED_TAXONOMY`: an array of six top-level entries `{ name, slug, description?, subCategories?: { name, slug }[] }` exactly per specv2.md §1.2 — `AI Use Cases`/`ai-use-cases` with the five children (`AI for Creators & Media`/`ai-for-creators-media`, `AI for Real Estate & Sales`/`ai-for-real-estate-sales`, `AI for E-commerce & Business`/`ai-for-e-commerce-online-business`, `AI for Marketers & Agencies`/`ai-for-marketers-agencies`, `AI for Finance & Legal`/`ai-for-finance-legal-consulting`), plus `AI Content & Copywriting`/`ai-content-copywriting`, `AI Video & Image Generation`/`ai-video-image-generation`, `AI Automation & Agents`/`ai-automation-agents`, `AI Marketing & Sales`/`ai-marketing-sales`, `AI Audio & Code`/`ai-audio-code`.
       - `seedTaxonomy()`: `connectToDatabase()`; for each entry `findOne({ slug })` then `create` only when absent (never update existing, never delete); for each child `findOne({ slug })` then `create({ category_id: parent._id, ... })` only when absent. Return `{ createdCategories, createdSubCategories }` counts. No `deleteMany`, no `updateOne` on existing docs.
       - A callable form with no top-level side effects so a vitest import does not connect eagerly.

    2. Create `scripts/seed-taxonomy.ts` as a thin CLI: import `seedTaxonomy`, run it, log the created counts, exit non-zero on throw (per the `scripts/seed-*.ts` precedent). Do not echo env values.

    3. Create `tests/lib/seed-taxonomy.test.ts`: run `seedTaxonomy()` on an empty memory DB; assert 6 categories and 5 sub-categories created, the exact slugs present, and each child's `category_id` points at `ai-use-cases`. Run it a second time and assert `createdCategories === 0` and `createdSubCategories === 0` with total counts unchanged (idempotency). Pre-create one ad-hoc category and one ad-hoc sub-category, run the seed, and assert both still exist unchanged (coexistence).

    4. Create `tests/api/categories-taxonomy.test.ts`: seed the taxonomy, invoke `GET /api/v1/cms/categories` with an admin token, assert the response contains the six top-level categories and that the `ai-use-cases` entry carries five `subCategories` with the expected slugs — the read path the CMS tree renders from.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/seed-taxonomy.test.ts tests/api/categories-taxonomy.test.ts</automated>
    <fails_when>non-zero exit code, "failed" in the vitest summary line, "0 passed" in the summary, or a second run reporting non-zero created counts</fails_when>
  </verify>
  <done>The V5.2 two-level taxonomy seeds idempotently and is served by the categories API for the CMS tree; a manual `npx tsx scripts/seed-taxonomy.ts` run produces the same tree (CMS-04, D-11, A3).</done>
  <acceptance_criteria>
    - `src/lib/seed-taxonomy.ts` exports `SEED_TAXONOMY` and `seedTaxonomy`; `scripts/seed-taxonomy.ts` exists as a `tsx` CLI entry
    - A first `seedTaxonomy()` run creates 6 categories and 5 sub-categories with the exact spec slugs
    - A second run reports `createdCategories === 0` and `createdSubCategories === 0`
    - A pre-existing ad-hoc category and sub-category survive the seed unchanged
    - `GET /api/v1/cms/categories` returns the 6 top-level categories and `ai-use-cases` carries 5 `subCategories`
    - `npx vitest run tests/lib/seed-taxonomy.test.ts tests/api/categories-taxonomy.test.ts` exits 0
  </acceptance_criteria>
  <reversibility rating="reversible">The seed only adds missing rows and never deletes; re-running or omitting it is safe and no schema/contract changes.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Editor category integrity — required Level-1, filtered/reset Level-2, GEO optional (D-09, D-12)</name>
  <files>src/app/admin/page.tsx</files>
  <read_first>
    - src/app/admin/page.tsx — the category block (~2226-2264): the Level-1 select (already resets `subCategoryId` on change at ~2233-2236) and the conditional Level-2 select gated on `selectedCategoryObj?.subCategories?.length > 0`; `selectedCategoryObj` derivation (~1778); the save handler's required-field guard and payload (~1840-1876)
    - .planning/phases/02-cms-end-to-end-v5-2/02-UI-SPEC.md — Copywriting rows for taxonomy (`Choose a primary category.`, `Sub-category (optional)`, category-with-no-children), Interaction Contract 5-7, UI Considerations E3
    - src/lib/cms-fetch.ts — the helper from plan 02-03 for any taxonomy fetch
  </read_first>
  <action>
    1. Add the D-12 save-time validation: when `categoryId` is empty, block the save and show the inline rose copy `Choose a primary category.` beneath the Level-1 select. `title`, `slug`, `content` remain the only other required fields, and `status` must be set (existing behavior). Do not add any requirement on the GEO fields — `focus_keyword`, `key_takeaways`, `entities`, `faq_schema` stay optional (D-09).

    2. Confirm and keep the existing behavior: the Level-2 label reads `Sub-category (optional)`, the select is filtered to `selectedCategoryObj.subCategories`, changing the parent resets `subCategoryId` to `''`, and a category with no children renders no sub-select at all (no empty select). If any of these is not currently true, correct it.

    3. Ensure the editor's GEO block remains prominent but non-blocking: empty `focusKeyword`/`keyTakeawaysText`/`entitiesText`/`faqRows` must not prevent save. Assert this by inspecting the save guard: it must only test title/slug/content/category/status.

    Do not restructure surrounding editor layout or move state out of `ArticleEditorForm` (HYG-02 boundary).
  </action>
  <verify>
    <automated>npx vitest run tests/api/articles-edit-roundtrip.test.ts; if ($?) { npx tsc --noEmit }</automated>
    <fails_when>non-zero exit, "failed" in the vitest summary line, or any tsc error reported</fails_when>
  </verify>
  <done>Save is blocked only on title/slug/content/category/status; the sub-category field is filtered, optional, and cleared on parent change; a childless category hides the sub-select; GEO fields never block a save (D-09, D-12).</done>
  <acceptance_criteria>
    - With no `categoryId`, the save handler blocks and surfaces `Choose a primary category.`
    - The GEO fields are not tested in the save guard (only title/slug/content/category/status)
    - The Level-2 label reads `Sub-category (optional)`; changing the parent sets `subCategoryId` to `''`
    - A category with zero children renders no sub-category select element
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: FAQ row editor alignment to the UI-SPEC contract (D-10)</name>
  <files>src/app/admin/page.tsx, tests/api/articles-edit-roundtrip.test.ts</files>
  <read_first>
    - src/app/admin/page.tsx — the FAQ builder (~2125-2164): `addFaqRow`/`removeFaqRow`/`updateFaqRow` (~1821-1833) and the `validFaq` filter used in the payload (~1856, 1874)
    - .planning/phases/02-cms-end-to-end-v5-2/02-UI-SPEC.md — Copywriting rows for the FAQ row editor (add/remove/empty/helper), Interaction Contract 6, UI Considerations E2
    - src/lib/faq-jsonld.ts — the builder from plan 02-02 (the render side this editor feeds)
  </read_first>
  <action>
    1. Align the FAQ row editor copy/structure to the UI-SPEC: add-row button reads `+ Add question`; row inputs are placeholdered `Question…` / `Answer…`; the remove control is an icon-only `Trash2` with `title="Remove this question"` and `aria-label="Remove FAQ row {index}"`; the zero-row empty state reads `No FAQ pairs yet. Add a question to embed FAQPage structured data on the public article.`; the helper line reads `Rows with an empty question or answer are ignored when saving.`. Keep the violet GEO hue language already used for this block.

    2. Keep the payload rule: `faqRows.filter((f) => f.question.trim() && f.answer.trim())` is what is sent as `faqSchema` — incomplete rows are dropped silently with no toast and no blocking (D-10).

    3. Persist the render contract in `tests/api/articles-edit-roundtrip.test.ts` (extend): `PUT` an article with one complete and one incomplete FAQ row (the client would have filtered; the API stores what it receives) and assert the stored `faq_schema` is exactly the filtered-complete set; assert zero FAQ rows stores an empty array and the article still saves.

    4. Confirm `updateFaqRow` writes immutably (clone-then-set) so React state updates are not aliased — repair only if it currently mutates in place.

    Manual UAT (record in summary): add two FAQ rows, leave one blank, save, reload the editor and confirm only the complete pair persisted; view the public article and confirm FAQPage JSON-LD reflects it (ties to plan 02-02).
  </action>
  <verify>
    <automated>npx vitest run tests/api/articles-edit-roundtrip.test.ts tests/lib/faq-jsonld.test.ts; if ($?) { npx tsc --noEmit }</automated>
    <fails_when>non-zero exit, "failed" in the vitest summary line, or any tsc error reported</fails_when>
  </verify>
  <done>The FAQ row editor matches the UI-SPEC copy and accessibility contract; incomplete rows are dropped silently; the stored `faq_schema` round-trips and feeds the FAQPage builder (D-10, CMS-04/CMS-02).</done>
  <acceptance_criteria>
    - The add-row control reads `+ Add question`; inputs are placeholdered `Question…` / `Answer…`
    - The remove control is icon-only with `title="Remove this question"` and `aria-label="Remove FAQ row {index}"`
    - The empty state reads `No FAQ pairs yet. Add a question to embed FAQPage structured data on the public article.`
    - `faqSchema` in the payload is `faqRows.filter((f) => f.question.trim() && f.answer.trim())`
    - A PUT with one complete and one incomplete pair stores exactly the complete pair; zero pairs stores `[]` and still saves
    - `npx vitest run tests/api/articles-edit-roundtrip.test.ts tests/lib/faq-jsonld.test.ts` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI seed → taxonomy collections | A data-writing script runs against the live database |
| admin editor → taxonomy/FAQ payload | Admin-authored taxonomy and FAQ text crosses into stored article + structured data |
| seeded taxonomy → public taxonomy routes | Category/sub-category slugs become public URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-22 | Tampering | seed wiping existing taxonomy | high | mitigate | upsert-only, no `deleteMany`/overwrite; coexistence assertion (Task 1) |
| T-02-23 | Information disclosure | credentials echoed by the seed CLI | medium | mitigate | script logs counts only, never env values (Task 1) |
| T-02-24 | Tampering | FAQ/taxonomy text reaching a render as markup | medium | mitigate | stored via the existing sanitized article write path; FAQ rendered only as JSON-LD through `serializeJsonLd` (plan 02-02) |
| T-02-25 | Tampering | documentation drift on the seeded slugs vs spec §1.2 | low | mitigate | exact slugs asserted in tests against the spec list (Task 1) |
| T-02-SC | Tampering | npm installs | high | mitigate | none — this plan installs zero packages (Research: zero-new-packages phase) |
</threat_model>

<verification>
- `npx vitest run tests/lib/seed-taxonomy.test.ts tests/api/categories-taxonomy.test.ts tests/api/articles-edit-roundtrip.test.ts` green
- `npx tsc --noEmit` clean
- Manual: `npx tsx scripts/seed-taxonomy.ts` twice → second run reports 0 created; CategoriesView shows the 6 categories with `AI Use Cases` expanded to 5 sub-categories
</verification>

<success_criteria>
- The V5.2 §1.2 two-level taxonomy is seeded idempotently and manageable in the CMS (CMS-04, ROADMAP criterion 4, D-11)
- A top-level category is required and the sub-category is optional/filtered in the editor (D-12)
- GEO fields stay optional; the FAQ row editor matches the UI-SPEC and feeds the FAQPage builder (D-09, D-10)
</success_criteria>

<output>
Create `.planning/phases/02-cms-end-to-end-v5-2/02-06-SUMMARY.md` when done
</output>
