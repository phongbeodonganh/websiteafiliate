---
phase: 02-cms-end-to-end-v5-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth.ts
  - src/app/api/v1/auth/logout/route.ts
  - src/app/api/v1/auth/me/route.ts
  - src/app/api/v1/cms/affiliate-links/route.ts
  - src/app/api/v1/cms/affiliate-links/[id]/route.ts
  - src/app/api/v1/cms/ai/generate-article/route.ts
  - src/app/api/v1/cms/ai/generate-takeaways/route.ts
  - src/app/api/v1/cms/articles/route.ts
  - src/app/api/v1/cms/articles/[id]/route.ts
  - src/app/api/v1/cms/blacklist/route.ts
  - src/app/api/v1/cms/blacklist/check/route.ts
  - src/app/api/v1/cms/blacklist/import/route.ts
  - src/app/api/v1/cms/blacklist/import-sheet-url/route.ts
  - src/app/api/v1/cms/blacklist/quick-blacklist/route.ts
  - src/app/api/v1/cms/categories/route.ts
  - src/app/api/v1/cms/categories/[id]/route.ts
  - src/app/api/v1/cms/click-logs/route.ts
  - src/app/api/v1/cms/dashboard/route.ts
  - src/app/api/v1/cms/insider/send-now/route.ts
  - src/app/api/v1/cms/insights/route.ts
  - src/app/api/v1/cms/settings/route.ts
  - src/app/api/v1/cms/sub-categories/route.ts
  - src/app/api/v1/cms/sub-categories/[id]/route.ts
  - src/app/api/v1/cms/subscribers/route.ts
  - src/app/api/v1/cms/subscribers/[id]/route.ts
  - src/app/api/v1/cms/upload/route.ts
  - src/app/api/v1/cms/users/route.ts
  - src/app/api/v1/cms/users/[id]/route.ts
  - tests/api/cms-auth-401.test.ts
  - tests/api/cms-auth-inactive.test.ts
  - tests/api/insider-admin.test.ts
autonomous: true
requirements: [AUTH-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A valid signed token belonging to a user whose DB `status` is 'inactive' is rejected with 401 on every authenticated CMS route — the lockout applies on the very next request, not at the 24h token expiry (D-15)"
    - "A valid signed token whose `userId` does not exist in the users collection is also rejected with 401 — a deleted account's token cannot ride out its 24h lifetime (A1)"
    - "A valid signed token whose `userId` is malformed or non-ObjectId is rejected with the route's normal 401/403 — the guard fails closed and never throws CastError into a 500 (information-leak / 500-vs-401 closure). Correspondingly, every test that signs a token for a principal expected to PASS the guard seeds an active user and signs with that document's real ObjectId `_id` — the fail-closed path covers only REJECTION cases and cannot make a PASS-case token succeed"
    - "A valid signed token for an existing, active user continues to pass the guard on every route (no over-rejection regression)"
    - "`getAuthUser(req)` is async and returns `Promise<AuthPayload | null>`; every one of its call sites awaits the result before testing truthiness — an un-awaited call is a Promise (always truthy) and would silently disable the guard"
    - "The parameterized `tests/api/cms-auth-401.test.ts` gate stays green through the conversion: every CMS route still rejects no-token / former-fallback-secret / logout-blacklisted-token requests with the same status it did before"
    - "The async guard does one indexed `_id` lookup of the User document per authenticated request; no other DB work is added to the request path"
  artifacts:
    - "src/lib/auth.ts — `getAuthUser` is async; performs a UserModel lookup by userId after token verification and returns null for a missing or inactive user, and returns null (not a throw) for a malformed/non-ObjectId userId"
    - "all 27 route files — `const user = await getAuthUser(req)`"
    - "tests/api/cms-auth-inactive.test.ts — NEW: inactive-user 401, missing-user 401, active-user pass, per route class"
    - "tests/api/cms-auth-401.test.ts — MOD: the control test seeds an existing active user for its `userId` before asserting non-rejection"
    - "tests/api/insider-admin.test.ts — MOD: seed ONE active admin `UserModel` and sign the test-2/test-3 admin tokens with that document's `_id`; keep the `'editor-id'` editor token as a genuine fail-closed rejection"
  key_links:
    - "verifyToken (sync, pure) → unchanged; getAuthUser (async) → verifyToken then a CastError-safe UserModel lookup by userId → status gate"
    - "every route guard clause → `const user = await getAuthUser(req)` → `if (!user) return 401/403`"
    - "tests/api/cms-auth-401.test.ts control case → seeded active user → guard passes → handler body runs"
  assumptions:
    - "[AUTH-04/adjacency|flagged-assumption] 'Inactive' and 'missing' are one rejection class: both return null from getAuthUser and therefore the same 401/403 status the route already returns for an unrecognized principal — no new status code is introduced."
    - "[AUTH-04/malformed-id|flagged-assumption] A malformed/non-ObjectId userId is treated as 'missing' (null), not as a server error: Mongoose's `findById` CastError is caught at the guard and mapped to rejection. This prevents a 500 from leaking that the id shape was invalid. Consequence, applied in this plan: the fail-closed path only covers REJECTION cases (e.g. `insider-admin.test.ts` test 1's `'editor-id'` editor token). Every test whose principal must PASS the guard — including `insider-admin.test.ts` tests 2 and 3 — must seed an active user and sign with that document's real ObjectId `_id`."
  prohibitions:
    - statement: "The auth guard must never treat a verified signature as sufficient authorization — the user must exist and be active in the DB"
      status: flagged-unverified
      verification: none
    - statement: "No route may be left calling `getAuthUser(req)` without awaiting it, and no route may reintroduce a route-local JWT/secret fallback alongside the canonical guard"
      status: flagged-unverified
      verification: none
    - statement: "A 401 gate on a route must not be weakened to 403-or-200 or removed to make the async conversion pass"
      status: flagged-unverified
      verification: none
---

<objective>
Convert the canonical auth guard `getAuthUser` from synchronous (signature-only) to asynchronous with a database-backed active-status check, and await it at every call site. This closes the existing-token window for deactivated and deleted accounts (D-15, assumption A1) across the entire CMS surface at once.

Purpose: A deactivated account currently keeps a working session until its 24h JWT expires. Enforcing `status` in the single canonical guard locks the account out on the next request without duplicating the check in 27 routes.

Output: async `src/lib/auth.ts`, awaited guards in all 27 route files, an updated 401-gate control test, an updated `insider-admin.test.ts` (seeded active admin), and a new inactive/missing-user regression suite.
</objective>

<execution_context>
@C:/Users/PC/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/PC/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cms-end-to-end-v5-2/02-CONTEXT.md
@.planning/phases/02-cms-end-to-end-v5-2/02-RESEARCH.md

Auth storage note: JWT stays in localStorage (Phase 1 D-15 lock) — this plan changes only the server-side guard, never the token transport or storage.
</context>

## Artifacts this phase produces (plan 01)

- `src/lib/auth.ts` — MOD: `getAuthUser(req): Promise<AuthPayload | null>` (async), CastError-safe DB status/missing check
- 27 route files under `src/app/api/v1/**` — MOD: `await getAuthUser(req)`
- `tests/api/cms-auth-inactive.test.ts` — NEW: inactive/missing/active regression matrix
- `tests/api/cms-auth-401.test.ts` — MOD: control case seeds an active user
- `tests/api/insider-admin.test.ts` — MOD: seed one active admin user; sign the admin tokens with its `_id` (test 1's `'editor-id'` editor token stays a fail-closed rejection)

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — deactivated user is locked out end-to-end (guard → route → 401)</name>
  <files>src/lib/auth.ts, all 27 route files listed in frontmatter, tests/api/cms-auth-401.test.ts, tests/api/cms-auth-inactive.test.ts, tests/api/insider-admin.test.ts</files>
  <read_first>
    - src/lib/auth.ts — whole file: `AuthPayload` shape (userId/username/role, no status), `getJwtSecret()` lazy getter, `verifyToken` (sync, blacklist + signature only), `getAuthUser` (sync, lines 47-55 — the conversion target)
    - src/lib/db/models.ts — the exported `UserModel` and `UserSchema`: `username` (required, unique), `password_hash` (required), `role` (enum, default 'author'), `name?`, `status` (enum, default 'active'), `avatar?`. A seeded active admin must supply `username`, `password_hash`, `role: 'admin'`, `status: 'active'` or `create` throws on validation
    - src/app/api/v1/cms/articles/route.ts — the guard-clause shape every route repeats: `const user = getAuthUser(req); if (!user) return 401`
    - src/app/api/v1/cms/insider/send-now/route.ts — the guard is called OUTSIDE any try/catch (line 9): a throwing `getAuthUser` becomes an unhandled 500, not a 403. Its admin branch → 503 when `INSIDER_CRON_SECRET` unset (lines 14-20), → 200 path when set
    - tests/api/insider-admin.test.ts — test 1 signs an editor token `'editor-id'` (line 39, expects 403 — a genuine fail-closed rejection). Tests 2 (line 48, expects 503) and 3 (line 79, expects 200) sign admin tokens with the non-ObjectId literal `'admin-id'` and seed NO user — both MUST be changed to sign with a seeded active admin's `_id`. The file already calls `connectToDatabase()` (line 63) and imports `Types`, `SubscriberModel`, `ArticleModel`
    - tests/api/cms-auth-401.test.ts — whole file: the ROUTES table, `expectedNoAuthStatus` per route, and the control test at ~lines 447-465 that signs `userId: 4` with no seeded user and asserts `not.toBe(401)`
    - src/app/api/v1/auth/login/route.ts — the existing login-path inactive check (`status === 'inactive'` → 403); the guard closes the *existing-token* window the login path cannot
    - tests/setup.ts — MongoMemoryServer lifecycle; the suite has a real DB available
    - .planning/phases/02-cms-end-to-end-v5-2/02-RESEARCH.md — section "C-2: D-15 needs `getAuthUser` to become async"
  </read_first>
  <action>
    This conversion is atomic by necessity: `getAuthUser` becoming async turns every un-updated call site into `Promise | null` (truthy), which would disable the guard. Update the guard, every call site, and the affected tests in this one task so the suite never sits red between edits.

    1. In `src/lib/auth.ts`: import `UserModel` from `@/lib/db/models`, `connectToDatabase` from `@/lib/db/mongodb`, and (for the id-shape guard) `Types` from `mongoose`. Change the signature to `export async function getAuthUser(req: Request): Promise<AuthPayload | null>`. Keep `verifyToken` synchronous and unchanged. After `verifyToken(token)` returns a payload:
       - **Fail-closed on a malformed/missing userId (CastError closure):** before touching the DB, treat a `userId` that is not a valid ObjectId as "missing" and return `null`. Choose a mechanism and state it in a comment: either a `Types.ObjectId.isValid(String(payload.userId))` pre-check, or wrapping the `findById` in a try/catch that maps Mongoose's `CastError` to `null`. Either is acceptable; the required behavior is that a non-ObjectId userId yields `null`, never a thrown error — a thrown guard would surface as a 500 from routes that call it outside a try block (e.g. `insider/send-now`) and would leak that the id shape was invalid (500-vs-401 information leak). Do NOT let `findById` run unguarded.
       - Then `await connectToDatabase()` and `UserModel.findById(payload.userId)`. Return `null` when the document is missing OR `userDoc.status !== 'active'`. Return the payload only for an existing active user. Keep the early `null` for a missing/malformed Authorization header. Add a one-line `//` comment stating the inactive/missing/malformed lockout intent (Vietnamese rationale comments match repo convention). Keep the lazy `getJwtSecret()` getter untouched.

    2. In each of the 27 route files, change `const user = getAuthUser(req);` to `const user = await getAuthUser(req);` (and any other local alias of the same call, e.g. `currentUser`). Handlers are already `async`, so no handler signature changes. Do NOT alter any status code, role check, or message.

    3. In `tests/api/cms-auth-401.test.ts` control test (~lines 447-465): seed an active `UserModel` document and sign the fresh token with that document's `_id` as `userId`, so the guard's DB lookup succeeds and the assertion `expect(res.status).not.toBe(401)` remains meaningful. Do not change the ROUTES table or any `expectedNoAuthStatus`. (Note: the tokens in this file that use `userId: 1/2/3` and the non-ObjectId legacy ids must still be rejected via the malformed/missing path — they land on the rejection assertions, not on a 500.)

    4. Fix `tests/api/insider-admin.test.ts` to seed its pass-case principal. Import `UserModel` from `@/lib/db/models`. Seed the admin inside test 2 and test 3 individually (NOT a shared `beforeEach` with one fixed username): after `await connectToDatabase()`, do `const admin = await UserModel.create({ username: 'insider-admin', password_hash: 'x', role: 'admin', status: 'active' })`. Add this same `connectToDatabase()` call to test 2 (test 3 already calls it at line 63). Because `username` is `unique`, give each test a distinct username (e.g. `'insider-admin-cron-unset'` for test 2, `'insider-admin-cron-set'` for test 3) so the two creates cannot collide in the shared MongoMemoryServer. `password_hash` is required; `role: 'admin'` and `status: 'active'` are required for the token to pass the guard's admin branch. Capture the created document's `_id` (a real ObjectId) and sign the test-2 (line 48) and test-3 (line 79) admin tokens with `userId: admin._id.toString()` instead of the literal `'admin-id'`. Leave test 1's `'editor-id'` editor token (line 39) exactly as-is: it is a non-ObjectId literal with no seeded user, so the guard fails closed → `!user || role !== 'admin'` → 403, the assertion it already makes. Resulting outcomes:
       - test 1: unauthenticated → 403; `'editor-id'` editor token → 403 (guard fail-closed rejection, unchanged).
       - test 2: seeded active admin's `_id` + `INSIDER_CRON_SECRET` unset → guard passes the admin branch → 503.
       - test 3: seeded active admin's `_id` + `INSIDER_CRON_SECRET` set → guard passes → 200 path with the existing digest assertions.
       Do NOT change any assertion status. The fail-closed mechanism only guarantees rejection; making a pass-case token succeed requires the seeded active user.

    5. Create `tests/api/cms-auth-inactive.test.ts`: seed three users — one `active` admin, one `inactive` admin, and no document for a third (valid ObjectId) `userId`. Sign a valid canonical token for each. Assert:
       - inactive user's token → 401 on `GET /api/v1/cms/articles` and on `GET /api/v1/auth/me`.
       - missing user's token → 401 on the same two routes.
       - a malformed/non-ObjectId userId token → the route's rejection status (401/403), explicitly NOT 500 (guard fail-closed regression).
       - active admin's token → NOT 401/403 on `GET /api/v1/cms/articles`.

    Note: the global `getAuthUser` gate test must remain green. If any route's `expectedNoAuthStatus` moves, that is a regression to fix in the route, not in the expectation.
  </action>
  <verify>
    <automated>npm test; if ($?) { npx tsc --noEmit }</automated>
    <fails_when>non-zero exit code, a line containing "failed" in the vitest summary, an inactive/missing/malformed-token case receiving a status other than the route's rejection code (or a 500), or any tsc error</fails_when>
  </verify>
  <done>`getAuthUser` is async, fail-closed on inactive/missing/malformed userId (never throws); all 27 call sites await it; a deactivated user's valid token is 401 on a real route; `insider-admin.test.ts` seeds an active admin so its pass-case (503/200) tests exercise the guard's success path while its editor token remains a fail-closed 403; the full suite and typecheck are green.</done>
  <acceptance_criteria>
    - `src/lib/auth.ts` exports `async function getAuthUser(req: Request): Promise<AuthPayload | null>` and contains a malformed/non-ObjectId userId guard (pre-check or CastError catch) — `findById` is never allowed to throw CastError to the caller
    - `grep -n "getAuthUser(req)" src/lib/auth.ts` shows the `async` declaration; no route file contains a non-awaited `getAuthUser(req)` truthiness test
    - In `tests/api/cms-auth-inactive.test.ts`, an inactive-user token and a missing-user token each produce HTTP 401 on `GET /api/v1/cms/articles`; a malformed-id token produces 401/403 (never 500); the active-admin token does not
    - `tests/api/insider-admin.test.ts` seeds an active admin and signs the test-2/test-3 tokens with that user's `_id`; its three cases resolve to test 1: editor `'editor-id'` → 403 (fail-closed), test 2: seeded admin + no cron secret → 503, test 3: seeded admin + cron secret → 200
    - `npm test` exits 0 with 0 failed tests (the parameterized 401 gate and all three `tests/api/insider-admin.test.ts` cases are green), and `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <reversibility rating="costly">Changing a shared lib's signature invalidates its public contract for every consumer; reverting means re-editing 28 files and re-deriving which routes depend on the DB-backed check. The behavioral intent (D-15) is locked, so the cost is in the mechanical revert, not the decision.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Expansion — inactive/missing matrix across route classes + full-suite proof</name>
  <files>tests/api/cms-auth-inactive.test.ts, tests/api/cms-auth-401.test.ts</files>
  <read_first>
    - tests/api/cms-auth-inactive.test.ts — the just-created matrix (extend, don't rebuild)
    - tests/api/cms-auth-401.test.ts — the ROUTES table; reuse its route inventory so the inactive matrix covers the same classes
    - src/lib/auth.ts — the final guard
    - .planning/phases/02-cms-end-to-end-v5-2/02-RESEARCH.md — "Pitfall 5: D-15's DB lookup changes the meaning of a valid token"
    - .planning/codebase/CONVENTIONS.md — response envelope + status mapping the matrix must assert against
  </read_first>
  <action>
    1. Extend `tests/api/cms-auth-inactive.test.ts` so the inactive-token 401 is proven across the route *classes*, not one example: at minimum a plain-guard read route (`GET /cms/dashboard`), an admin-only write route (`POST /cms/users` — assert the route's own 403 for a non-privileged principal is NOT confused with the guard's 401; seed the inactive user as `admin` so the guard is the only reason for rejection), a resource route (`GET /cms/articles/:id`), and `POST /api/v1/auth/logout`. Each case: sign with an existing-but-inactive user and assert the guard's rejection status.

    2. Add a "reactivation restores access" case: create an inactive user, prove 401; flip `status` to `'active'` in the DB; re-issue a token (or reuse a fresh one) and prove the guard now passes on `GET /api/v1/auth/me`. This pins that the check is live DB state, not a cached or token-baked value.

    3. Add a source-contract assertion in the same file: walk `src/app/api/v1/**/route.ts` and assert every file containing `getAuthUser(` also contains `await getAuthUser(` — an un-awaited call site is an auth-bypass regression and must fail loudly.

    4. Run the full suite; the security-regression gate and the 401 gate must both stay green.
  </action>
  <verify>
    <automated>npm test; if ($?) { npx tsc --noEmit }</automated>
    <fails_when>non-zero exit, a "failed" line in the vitest summary, or any tsc error reported</fails_when>
  </verify>
  <done>The inactive/missing lockout is proven on read, write, resource and logout routes; reactivation restores access; every `getAuthUser` call site is proven awaited; full test suite + typecheck green.</done>
  <acceptance_criteria>
    - `tests/api/cms-auth-inactive.test.ts` contains cases for `GET /cms/dashboard`, `POST /cms/users`, `GET /cms/articles/:id`, and `POST /api/v1/auth/logout`
    - A reactivation case flips a user's status to `active` and asserts the same route now passes the guard
    - A source walk over `src/app/api/v1/**/route.ts` asserts every file containing `getAuthUser(` also contains `await getAuthUser(`
    - `npm test` exits 0 and `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bearer token → auth guard | A cryptographically valid token is untrusted until the principal's live DB status is confirmed |
| deactivated/deleted account → CMS surface | A stale session must not retain access after the account is disabled |
| guard → 27 route handlers | Every route delegates authorization to the one canonical guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | stale token of a deactivated user | high | mitigate | DB status check inside `getAuthUser`; inactive → null → route's 401/403 (Task 1) |
| T-02-02 | Spoofing | token of a deleted user (missing document) | high | mitigate | missing document → null → 401 (Task 1, assumption A1) |
| T-02-03 | Elevation of Privilege | un-awaited guard call site silently disables auth | critical | mitigate | atomic conversion + source-contract test walking all `route.ts` files (Task 2) |
| T-02-04 | Tampering | 401 gate weakened to make the conversion pass | high | mitigate | ROUTES table `expectedNoAuthStatus` preserved verbatim; the control test and `insider-admin.test.ts` pass-cases seed a real active user instead of weakening the guard (Task 1) |
| T-02-05 | Denial of Service | new DB read per authenticated request | low | accept | one indexed `_id` lookup; documented cost, not a degradation path at this scale |
| T-02-06 | Information disclosure | malformed/non-ObjectId userId → Mongoose CastError → 500 from a guard called outside a try block | medium | mitigate | guard validates/handles the id shape and returns `null` (fail-closed) instead of throwing; legacy non-ObjectId tokens reject with 401/403, never 500 (Task 1) |
| T-02-SC | Tampering | npm installs | high | mitigate | none — this plan installs zero packages (Research: zero-new-packages phase) |
</threat_model>

<verification>
- `npm test` full suite green (includes `tests/api/cms-auth-401.test.ts`, `tests/api/cms-auth-inactive.test.ts`, `tests/api/insider-admin.test.ts`, `tests/api/auth-login.test.ts`), `npx tsc --noEmit` clean
- Manual spot check (optional): deactivate a test account, confirm the next request with its existing token is rejected
</verification>

<success_criteria>
- A valid token for an inactive, deleted, or malformed-id user is rejected on the next request (D-15, A1) — the guard never throws CastError into a 500
- No route's auth behavior changes for active users; the parameterized 401 gate is unchanged and green
- Guard is async and every call site awaits it, enforced by a source-contract test
</success_criteria>

<output>
Create `.planning/phases/02-cms-end-to-end-v5-2/02-01-SUMMARY.md` when done
</output>
