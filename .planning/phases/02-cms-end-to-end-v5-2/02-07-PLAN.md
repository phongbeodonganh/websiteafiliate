---
phase: 02-cms-end-to-end-v5-2
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/v1/cms/blacklist/route.ts
  - src/app/admin/page.tsx
  - tests/api/blacklist-post-rbac.test.ts
  - tests/api/admin-blacklist-check-token.test.ts
autonomous: true
requirements: [AUTH-03, AFF-04]
gap_closure: true
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A non-admin token can no longer blacklist a domain: POST /api/v1/cms/blacklist rejects editor and author principals with the sibling 401 contract and the retroactive sweep never runs, so no campaign is mass-deactivated (closes CR-03 / VERIFICATION.md truth 9)."
    - "The affiliate-link blacklist pre-check sends the bearer token and fails closed: when the check is unavailable (401/403/5xx/network) a visible warning stays on screen instead of being cleared, so a known-blacklisted base_url cannot be saved silently (closes CR-01 / VERIFICATION.md truth 10)."
    - "The pre-existing no-token contract for POST /api/v1/cms/blacklist stays at 401 — the guard is added without weakening tests/api/cms-auth-401.test.ts."
  artifacts:
    - path: "src/app/api/v1/cms/blacklist/route.ts"
      provides: "POST handler carrying the same combined admin guard as the sibling DELETE"
    - path: "src/app/admin/page.tsx"
      provides: "handleCheckAffUrl routed through cmsFetch with a token and a fail-closed branch"
    - path: "tests/api/blacklist-post-rbac.test.ts"
      provides: "role-based POST regression: editor/author rejected, admin succeeds, no sweep side-effect"
    - path: "tests/api/admin-blacklist-check-token.test.ts"
      provides: "source contract pinning the pre-check through cmsFetch and the fail-closed branch"
  key_links:
    - "handleCheckAffUrl -> cmsFetch -> /api/v1/cms/blacklist/check with Authorization: Bearer attached"
    - "POST /cms/blacklist guard -> sweepRetroactiveBlacklist is unreachable for a non-admin principal"
  assumptions:
    - "AFF-04 idempotency: repeating a rejected non-admin POST stays rejected — the guard is evaluated on every request before any side effect."
    - "AUTH-03 encoding/adjacency: a malformed or missing Authorization header is treated as no principal (401), never as an admin."
    - "AFF-04 concurrency: concurrent admin POSTs are not serialized by this change; the guard only gates admission, it does not alter sweep batching."
  prohibitions:
    - statement: "UI hiding is not access control — the blacklist tab being hidden from editors is not sufficient; the POST route itself must reject a non-admin principal."
      status: flagged-unverified
    - statement: "No destructive global sweep may run for a principal that is not an admin."
      status: flagged-unverified
    - statement: "The blacklist pre-check must never report a blacklisted URL as safe because its own auth failed."
      status: flagged-unverified
---

<objective>
Close the two blacklist/token defects that left the phase's RBAC and affiliate-interceptor guarantees untrue: CR-03 (POST /api/v1/cms/blacklist has no role guard, so any authenticated user can blacklist a domain and globally sweep matching campaigns) and CR-01 (the affiliate-link pre-check calls the now-authenticated blacklist/check route with no bearer token, always 401s, and clears its own warning — silently failing open).

Purpose: restore the "correct RBAC" phase goal on the most destructive blacklist write and re-arm the affiliate-link interceptor that the Phase 02-01 auth swap silently disabled. Both gaps are on the blacklist/token concern and share the blacklist endpoint, so they are planned together.

Output: an admin-guarded POST handler with a role-based regression test; a token-bearing, fail-closed pre-check with a source-contract test.
</objective>

<execution_context>
@C:/Users/PC/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/PC/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cms-end-to-end-v5-2/02-VERIFICATION.md
@.planning/phases/02-cms-end-to-end-v5-2/02-REVIEW.md
@.planning/phases/02-cms-end-to-end-v5-2/02-PATTERNS.md
@.planning/phases/02-cms-end-to-end-v5-2/02-CONTEXT.md
@src/app/api/v1/cms/blacklist/route.ts
@src/app/admin/page.tsx
@src/lib/cms-fetch.ts

This is a gap-closure plan on already-executed work. Plans 02-01..02-06 are complete; do NOT re-plan them. The guard is added to the most destructive blacklist write (CR-03, same concern as AUTH-03/AFF-04) and the client pre-check is re-wired through the shared auth helper (CR-01). Per Phase 1 D-15 / plan 02-01, `getAuthUser` is already async and DB-backed — reuse it, never add a route-local guard.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Close CR-03 — admin-only guard on POST /api/v1/cms/blacklist, proven end-to-end through the sweep</name>
  <files>src/app/api/v1/cms/blacklist/route.ts, tests/api/blacklist-post-rbac.test.ts</files>
  <read_first>
    - src/app/api/v1/cms/blacklist/route.ts (POST at :37-42 gates on identity only; the sibling DELETE at :91-94 is the canonical combined guard to copy)
    - tests/api/cms-rbac-affiliate-403.test.ts (analog: seedScenario creating real active UserModel docs, signToken, direct handler invocation, DB side-effect assertion)
    - tests/api/cms-auth-401.test.ts:176 (pins POST /cms/blacklist no-token → 401; the guard must preserve this)
    - .planning/phases/02-cms-end-to-end-v5-2/02-PATTERNS.md (CR-03 section: "Choose the same-file DELETE form")
    - src/lib/auth.ts:58-81 (the canonical async DB-backed guard; reuse, do not duplicate)
  </read_first>
  <action>
    In src/app/api/v1/cms/blacklist/route.ts, replace the identity-only gate at the top of the POST handler with the same combined sibling guard already used by the DELETE handler in the same file (:91-94): call `const user = await getAuthUser(req)` and reject with `{ status: 'error', message: 'Unauthorized' }` and HTTP 401 when the principal is missing OR its role is not the literal `'admin'`. Use this same-file combined form deliberately — a split guard that returns a separate 403 for a non-admin would diverge from every other blacklist write (quick-blacklist, import, import-sheet-url, re-sweep, DELETE) and is unnecessary; the combined form keeps the no-token path at 401 and satisfies tests/api/cms-auth-401.test.ts:176 unchanged. Do NOT introduce a route-local token/identity check beyond the canonical awaited `getAuthUser(req)` call (that would regress the single-canonical-guard contract pinned by tests/api/cms-auth-inactive.test.ts). The guard must sit before `await req.json()` so a rejected principal cannot even parse a body.

    Then create tests/api/blacklist-post-rbac.test.ts, mirroring the two-describe structure of tests/api/cms-rbac-affiliate-403.test.ts:25-198. seedScenario(): `connectToDatabase()`; create three UserModel docs with status `'active'` and roles admin/editor/author (use a `Date.now()`/`Math.random()` suffix in usernames so parallel files do not collide); create an AffiliateLinkModel campaign whose base_url is on a unique test domain (for example `https://badsite-<suffix>.com/campaign`) with `status` left at its default active value; sign three tokens with `signToken({ userId, username, role })`. Add a `jsonRequest(method, token, body)` factory building `new Request('http://localhost/api/v1/cms/blacklist', { method, headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify(body) })`, and import `POST as blacklistPOST` from the route module. Describe block 1 ("non-admin is rejected and no sweep runs"): editor POST and author POST with `{ websiteUrl: 'https://badsite-<suffix>.com', reason: 'x' }` each assert `res.status === 401`, assert the response envelope `status === 'error'`, and assert the seeded campaign is still active via a fresh `AffiliateLinkModel.findById(campaign._id)` (proving `sweepRetroactiveBlacklist` never executed). Describe block 2 ("the gate is role-based, not blanket"): an admin POST with the same body asserts HTTP 200, envelope `status === 'success'`, a new BlacklistModel document exists for the domain, and the matching campaign now reads `'blacklisted'`.
  </action>
  <acceptance_criteria>
    - POST handler in src/app/api/v1/cms/blacklist/route.ts rejects an editor token with HTTP 401 and the envelope `{ status: 'error', message: 'Unauthorized' }`.
    - POST handler rejects an author token with HTTP 401 and the same envelope.
    - A non-admin POST leaves the seeded active campaign's status unchanged (sweep not invoked).
    - An admin POST with a valid `websiteUrl` + `reason` returns HTTP 200 with `{ status: 'success' }` and flips the matching active campaign to `'blacklisted'`.
    - The no-token path for POST /cms/blacklist remains 401 (tests/api/cms-auth-401.test.ts:176 still passes).
    - `npx vitest run tests/api/blacklist-post-rbac.test.ts tests/api/cms-auth-401.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/api/blacklist-post-rbac.test.ts tests/api/cms-auth-401.test.ts</automated>
    <fails_when>non-zero exit, "failed" in the vitest summary line, or "0 passed" in the summary</fails_when>
  </verify>
  <done>POST /api/v1/cms/blacklist rejects non-admin principals with 401 and never runs the retroactive sweep for them; the admin path still creates the entry and sweeps; the pre-existing no-token 401 gate is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Close CR-01 — route the affiliate-link pre-check through cmsFetch and fail closed</name>
  <files>src/app/admin/page.tsx, tests/api/admin-blacklist-check-token.test.ts</files>
  <read_first>
    - src/app/admin/page.tsx (handleCheckAffUrl at :517-544 — the raw fetch call and its else/catch that clear the warning)
    - src/app/admin/page.tsx:615-645 (canonical in-file cmsFetch call-site shape used by the sheet-import handler)
    - src/lib/cms-fetch.ts (:9-11 discriminated result union; :68-119 never throws on HTTP error and maps 401 to session copy)
    - tests/api/cms-auth-inactive.test.ts:189-219 (analog: readFileSync source-walk technique)
    - .planning/phases/02-cms-end-to-end-v5-2/02-PATTERNS.md (CR-01 section: fail closed on !result.ok)
  </read_first>
  <action>
    Rework `handleCheckAffUrl` in src/app/admin/page.tsx (~:517-544). Instead of the raw fetch that sends only a Content-Type header, obtain `const token = localStorage.getItem('token')` and call the shared helper: `const result = await cmsFetch<{ isBlacklisted: boolean; projectName?: string; matchedDomain?: string; reason?: string; blockedCountries?: string[] }>('/api/v1/cms/blacklist/check', { method: 'POST', body: { url: urlStr }, token })`. The helper attaches the bearer automatically (shape at :615-645). On `!result.ok`, FAIL CLOSED: call `setAffUrlBlacklistError({ isError: true, projectName: '—', matchedDomain: urlStr, reason: 'Blacklist check unavailable. Retry before saving.', blockedCountries: [] })` and `return` — a failed check must never clear the warning or silently look safe. When `result.ok` is true, set the existing warning state only if `result.data?.isBlacklisted` is truthy; clear it (with the existing clear call) only when the check succeeded and returned a non-blacklisted result. Keep the early-return short-circuit for an empty/too-short URL. Do NOT widen this change to other handlers or to the shared helper.

    Then create tests/api/admin-blacklist-check-token.test.ts as a source contract, mirroring the readFileSync technique at tests/api/cms-auth-inactive.test.ts:189-219 and the ordering idiom at tests/api/cms-rbac-affiliate-403.test.ts:224-232. Read `src/app/admin/page.tsx` with `readFileSync(join(process.cwd(), 'src/app/admin/page.tsx'), 'utf8')`; slice the handler body from the `const handleCheckAffUrl` declaration up to the next `const ` declaration at the same indentation; assert the slice contains `cmsFetch(` and references the blacklist check endpoint path; assert the slice does not contain a direct bare `fetch(` call to that endpoint (the string-contract check the executor spells in the test file itself); assert the slice contains the fail-closed assignment guarded by a `!result.ok` check. Keep it a source contract — the suite runs `environment: 'node'` with no jsdom (VERIFICATION.md human-verification #2).
  </action>
  <acceptance_criteria>
    - handleCheckAffUrl calls `cmsFetch` targeting `/api/v1/cms/blacklist/check` with a `POST` method, a `{ url }` body, and a token from localStorage.
    - The handler no longer performs a bare fetch with only a Content-Type header against that endpoint (pinned by tests/api/admin-blacklist-check-token.test.ts).
    - On `!result.ok` the handler sets a non-null blacklist-warning state and returns without clearing the warning.
    - The warning is cleared only when the check succeeds and the result is not blacklisted.
    - `npx vitest run tests/api/admin-blacklist-check-token.test.ts` exits 0 and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/api/admin-blacklist-check-token.test.ts</automated>
    <fails_when>non-zero exit, "failed" in the vitest summary line, or "0 passed" in the summary</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit or any "error TS" line in the output</fails_when>
  </verify>
  <done>The affiliate-link pre-check sends the bearer token, and any unavailable/failed check keeps a visible warning instead of clearing it — a blacklisted base_url can no longer be saved silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated-CMS-user → POST /api/v1/cms/blacklist | An editor/author principal crosses here and can trigger a global campaign deactivation |
| admin browser → POST /api/v1/cms/blacklist/check | Untrusted client-typed URL string crosses into a blacklist lookup whose auth status drives a safety warning |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | POST /api/v1/cms/blacklist (route.ts:37-42) | high | mitigate | Add the sibling combined admin guard before body parsing; regression test proves editor/author 401 and no sweep side effect |
| T-02-02 | Tampering | sweepRetroactiveBlacklist reachability for a non-admin | high | mitigate | Guard is placed before `req.json()` and before any DB write, so a rejected principal cannot reach the sweep |
| T-02-03 | Spoofing | Authorization header absent/malformed on the blacklist POST | medium | mitigate | `getAuthUser` is the single canonical DB-backed guard; a missing principal maps to 401, never to admin |
| T-02-04 | Information Disclosure | pre-check 401/5xx body reaching the admin UI | low | accept | The check response is only used to drive a boolean warning banner; the fail-closed branch shows a fixed copy, not server detail |
| T-02-05 | Denial of Service | client re-typing triggers repeated check calls | low | accept | The check fires on URL input change only; the route is authenticated and the existing rate-limit surface is unchanged by this plan |
| T-02-06 | Repudiation | a non-admin blacklist write leaving no trace | medium | mitigate | Rejected writes never persist; accepted writes record `created_by` from the authenticated admin principal (existing behavior preserved) |

No new packages are added by this plan, so no package-legitimacy checkpoint is required (schema-gate not applicable — this project uses MongoDB via Mongoose).
</threat_model>

<verification>
- `npx vitest run tests/api/blacklist-post-rbac.test.ts tests/api/cms-auth-401.test.ts` — guard is role-based and the no-token contract is intact.
- `npx vitest run tests/api/admin-blacklist-check-token.test.ts` — the pre-check goes through cmsFetch and fails closed.
- `npx tsc --noEmit` — no type regressions.
- Phase regression: `npx vitest run tests/api/cms-rbac-affiliate-403.test.ts tests/api/cms-auth-inactive.test.ts` still green.
</verification>

<success_criteria>
- CR-03 closed: an editor/author token gets 401 on POST /cms/blacklist and no campaign is swept.
- CR-01 closed: the pre-check carries the bearer token and fails closed on any non-ok result.
- Existing auth gates (no-token 401 inventory, awaited-guard source walk) remain green.
</success_criteria>

<output>
Create `.planning/phases/02-cms-end-to-end-v5-2/02-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Path | Kind | Created / Modified | Purpose |
|------|------|--------------------|---------|
| `src/app/api/v1/cms/blacklist/route.ts` | route handler | modified | POST gains the sibling combined admin guard (CR-03) |
| `src/app/admin/page.tsx` | client component | modified | `handleCheckAffUrl` re-wired through `cmsFetch` and fails closed (CR-01) |
| `tests/api/blacklist-post-rbac.test.ts` | test | created | role-based POST regression + no-sweep side-effect proof |
| `tests/api/admin-blacklist-check-token.test.ts` | test | created | source contract for token-bearing, fail-closed pre-check |

**Symbols introduced:** none new — the plan reuses `getAuthUser` (src/lib/auth.ts), `cmsFetch` / `CmsFetchResult` (src/lib/cms-fetch.ts), and the existing `setAffUrlBlacklistError` state setter. New test-local helpers only: `seedScenario`, `jsonRequest`.
