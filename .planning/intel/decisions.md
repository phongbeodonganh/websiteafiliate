# Synthesized Decisions (ADR intel)

Ingest set: 5 docs (3 SPEC, 2 DOC) — contains no ADR-classified documents.
0 decisions extracted, 0 locked (no source in this run carried `locked: true`).

No entries — fields absent rather than fabricated. If ADRs exist elsewhere in the repo, re-run ingest with an updated manifest.

Note: the absence of any LOCKED decision is why the relational-SQL-vs-MongoDB schema contradiction was auto-resolved as INFO (later revision governs) instead of escalating to a BLOCKER — see `.planning/INGEST-CONFLICTS.md`.
