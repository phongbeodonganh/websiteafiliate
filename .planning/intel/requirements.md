# Synthesized Requirements (PRD intel)

Ingest set: 5 docs (3 SPEC, 2 DOC) — contains no PRD-classified documents.
0 requirements extracted; no REQ IDs issued; no competing acceptance variants.

Functional-requirement content observed inside the three SPEC sources (auth/RBAC, article CMS, public frontend, affiliate link management and tracking) is captured in `constraints.md` per the SPEC → constraints type mapping — it was not re-routed here.

`requimentgg.md` was re-tagged DOC via the ingest manifest (previously UNKNOWN, low confidence) and is now extracted to `context.md`, not here.
